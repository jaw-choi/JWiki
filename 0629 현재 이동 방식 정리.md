# 전투 이동 & 경로탐색 시스템 분석

  

## 1. 경로 계산 타이밍 — "언제 BFS를 돌리는가"

  

### 한 줄 요약

> **0.1초마다 조건 체크, 조건 충족 시에만 BFS 재계산.**  

> 매 틱이 아니고, 한 번만도 아니고, 타일 도달 시도 아님.

  

### 상세 흐름

  

```

GameMode::Tick (매 프레임)

  └─ ULTFCombatManager::TickCombat()                   // CombatManager.cpp:219

       └─ TickMovement()                                // CombatManager.cpp:227

            ├─ UpdatePathIfNeeded(Unit)                 // 경로 계산 판단

            └─ ResolveMoveRequests(Requests)            // 실제 이동 확정

```

  

`UpdatePathIfNeeded` (`CombatManager.cpp:283`) 내부:

  

```cpp

Unit->PathRebuildTimer += DeltaSeconds;

if (Unit->PathRebuildTimer < PathRebuildInterval) return;  // 0.1초 미만이면 패스

```

  

0.1초가 지났어도 아래 3가지 조건 중 하나가 충족될 때만 BFS 실행:

  

| 조건 | 코드 위치 |

|------|-----------|

| 타겟이 이전 틱과 다른 타일로 이동함 (`bTargetMoved`) | CombatManager.cpp:296 |

| 경로가 막혀(`bPathBlocked`) 1초 재시도 타이머 만료 | CombatManager.cpp:306-311 |

| 현재 경로 스텝이 소진됨 (`GetDesiredNextTile() == -1`) | CombatManager.cpp:314 |

  

조건을 통과하면 `FindPathBFS`를 실제로 호출한다.

  

---

  

## 2. 이동 루프 구조 — 서버 논리 이동 vs 클라이언트 시각 보간

  

### 서버 (논리 이동)

  

```

TickCombat (매 프레임, HasAuthority() 가드)

  └─ ULTFUnitBase::TickCombat (LTFUnitBase.cpp:332)

       ├─ UpdatePathIfNeeded() → BFS 재계산 판단

       ├─ ResolveMoveRequests() → 다음 타일 확정 + 충돌 조정

       └─ MoveTowardTarget() → CharacterMovement::AddMovementInput

```

  

- 유닛이 타일 중심에서 **44cm 이내** 진입하면 도달로 판정 (`TileArrivalThreshold = 44.f`, `LTFUnitBase.h:246`)

- 도달 시 `bIsOccupied` 갱신, 이전 타일 해제 (`LTFUnitBase.cpp:576-591`)

  

### 클라이언트 (시각 보간)

  

```

OnRep_CurrentTileIndex() 수신

  └─ SetTimer(MovementTimerHandle, 0.02초, true)   // 50FPS 보간

       └─ UpdateMovementAnimation()

            └─ 타일 중심 도달 시 ClearTimer        // LTFChampionBase.cpp:175-206

```

  

- 서버에서 `CurrentTileIndex`(Replicated)가 변경되면 클라이언트는 이를 수신해 시각적 이동 보간만 수행

- 실제 AI 판단은 전혀 없음

  

### 타이머 상수 모음

  

| 상수 | 값 | 위치 |

|------|----|------|

| `PathRebuildInterval` | 0.1초 | LTFUnitBase.h:109 |

| `TargetCheckInterval` | 0.25초 | LTFUnitBase.h:251 |

| `TileArrivalThreshold` | 44cm | LTFUnitBase.h:246 |

| 클라이언트 보간 주기 | 0.02초 | LTFChampionBase.cpp:187 |

| bPathBlocked 재시도 | 1.0초 | CombatManager.cpp:309 |

  

---

  

## 3. BFS 경로 계산 상세 — `FindPathBFS`

  

파일: `LTFHexGridManager.cpp:1079`

  

```

UpdatePathIfNeeded

  1. CandidateTiles = GetTilesInRange(TargetTile, AttackRange)  // 공격 가능 위치 후보 전체

  2. 각 후보 타일마다 FindPathBFS 호출

  3. PathLen 최소 → DistToTarget 최소 → TileIndex 최소 순으로 BestGoalTile 선택

  4. ReserveCombatTile(BestGoalTile)  // 해당 타일 예약

  5. Unit->SetPath(BestPath)          // 경로 저장

```

  

BFS 내 장애물 판단 (`LTFHexGridManager.cpp:1128-1133`):

- **점유 타일** (`bIsOccupied == true`) → 통과 불가

- **다른 유닛이 예약한 타일** (`bReservedForCombat && CombatReservedBy != RequestUnit`) → 통과 불가

- GoalTile 자체는 점유돼 있으면 BFS 시작 전에 즉시 실패 반환

  

---

  

## 4. 타겟 변경 및 사망 시 경로 재계산

  

| 상황 | 처리 | 코드 위치 |

|------|------|-----------|

| 타겟이 다른 타일로 이동 | `bTargetMoved = true` → 다음 0.1초 체크 시 BFS 재계산 | CombatManager.cpp:296 |

| 더 가까운 적이 2칸 이상 가까워짐 | `InvalidatePath()` + `Searching` 상태 전환 | LTFUnitBase.cpp:413-429 |

| 타겟 사망 | `Target = nullptr` + `Searching` 상태 전환 | LTFUnitBase.cpp:359-362 |

  

`InvalidatePath()` (`LTFUnitBase.cpp:207-233`): GoalTile 예약 해제 + 경로 완전 초기화.

  

---

  

## 5. 서버 / 클라이언트 분리

  

### 서버 전용 실행 가드

  

| 위치 | 조건 |

|------|------|

| `LTFUnitBase.cpp:332` | `if (!HasAuthority()) return;` — TickCombat 전체 |

| `LTFChampionBase.cpp:140` | `if (!HasAuthority()) return;` — SetCurrentTileIndex |

| `LTFChampionBase.cpp:59` | `if (!HasAuthority()) return;` — InitializeByTID |

  

전투 AI(경로탐색, 공격 판정, 스탯 처리) = **서버 전용**.

  

### 복제 변수 (DOREPLIFETIME)

  

| 변수 | 위치 | 용도 |

|------|------|------|

| `CurrentTileIndex` | LTFChampionBase.cpp:87 | 클라이언트 이동 보간 트리거 |

| `OwnerPlayerIndex` | LTFChampionBase.cpp:88 | 팀 판별 |

| `StarLevel` | LTFChampionBase.cpp:89 | UI |

| `UnitTid` | LTFUnitBase.cpp:711 | 기물 식별 |

  

- `SetReplicateMovement(true)` (`LTFChampionBase.cpp:21`) — CharacterMovement 위치 자동 복제

- `SetNetUpdateFrequency(60.0f)` (`LTFChampionBase.cpp:22`) — 초당 60회 업데이트

  

---

  

## 6. 타일 점유 상태 갱신 타이밍

  

| 시점 | 처리 | 코드 위치 |

|------|------|-----------|

| **전투 시작** | `ResetCombatTileFlags` 전체 초기화 → 현재 위치 `bIsOccupied = true` | CombatManager.cpp:72-88 |

| **타일 도달** (44cm 이내 진입) | 이전 타일 `false`, 새 타일 `true`, 예약 해제 | LTFUnitBase.cpp:576-591 |

| **사망** | 현재 타일 + `ConfirmedNextTileIndex` 모두 `false`, GoalTile 예약 해제 | LTFUnitBase.cpp:655-676 |

  

> **중요:** 점유 갱신은 이동 **시작** 시점이 아니라 **물리적 도달** 시점임.

  

**예약 시스템** (`bReservedForCombat`)은 점유와 별개:

- `UpdatePathIfNeeded` → BestGoalTile 결정 시 `ReserveCombatTile` (CombatManager.cpp:383)

- `ResolveMoveRequests` → 다음 스텝 타일 확정 시 `ReserveCombatTile` (CombatManager.cpp:463)

- 도달 / 사망 / 경로 무효화 시 `ReleaseCombatReservation`

  

---

  

## 7. 충돌 처리 — `ResolveMoveRequests`

  

파일: `LTFCombatManager.cpp:398-483`

  

```

1. 속도 내림차순 정렬 (동속도면 TileIndex 낮은 쪽 우선)

2. 각 유닛 순서대로:

   a. 다음 타일이 bIsOccupied 또는 다른 유닛 예약 → 충돌

   b. 충돌 시: 인접 6방향 중 원래 목적지에 가장 가까운 빈 타일로 대체

   c. 대체 타일도 없으면 이번 틱 이동 포기

   d. 대체 이동 시 ClearPathOnly() → GoalTile 유지, 경로만 초기화

3. ClaimedTiles에 선점 등록

```

  

처리 순서: **크립(EnemyTeam) 먼저, 챔피언(AllyTeam) 나중** (`CombatManager.cpp:272-280`).

  

---

  

## 8. 우회 버그 원인 추측

  

### 현상

비어 있는 직선 경로가 있는데도 돌아서 이동함.

  

### 원인 후보

  

#### A. 점유 타임래그 (가장 유력)

```

유닛A: 타일 5 → 6 이동 시작 (아직 타일 6에 도달 안 함 → bIsOccupied = false)

유닛B: 0.1초 체크에서 BFS 계산 → 타일 6이 비어있다고 판단 → 같은 타일로 경로 확정

ResolveMoveRequests: 유닛A가 먼저 선점 → 유닛B는 대안 타일로 우회

유닛B의 다음 BFS(0.1초 후): 엉뚱한 위치에서 재계산 → 더 긴 경로 선택

```

점유 갱신이 "이동 시작"이 아닌 "물리 도달(44cm)" 시점이기 때문에 발생.

  

#### B. 예약된 통로 타일로 인한 BFS 우회

유닛A가 GoalTile이 아닌 **경유 타일**에 대한 예약은 없음. 하지만 여러 유닛이 같은 통로를 지날 때, ResolveMoveRequests에서 순서 앞선 유닛이 ClaimedTiles에 통로 타일을 등록하면 뒤따르는 유닛은 다음 0.1초 BFS 시 해당 타일이 bIsOccupied는 아니지만 이번 틱 ClaimedTiles로 막혀 대체 이동 → 그 위치에서 재BFS → 우회 경로 확정.

  

#### C. 여러 아군이 동일 방향으로 이동할 때 연쇄 우회

`CandidateTiles` 중 아군이 이미 예약(`bReservedForCombat`)한 타일은 `CanMoveToTile` 실패로 탈락. 좋은 위치를 먼저 예약한 유닛이 통로까지 막으면 뒤 유닛은 더 먼 GoalTile로 BFS → 실제로는 빈 경로가 있음에도 먼 우회 경로가 선택됨.

  

#### D. `ClearPathOnly` 후 0.1초 재계산 공백

충돌 우회 이동 후 `ClearPathOnly()`로 경로만 지워지는데, 다음 BFS는 최대 0.1초 후. 그 사이 틱에서 유닛은 GoalTile은 유지하지만 경로가 없어 멈추거나, 대체 이동한 위치에서 엉뚱한 방향으로 한 틱 더 이동할 수 있음.

  

### 공통 근본 원인

- BFS는 **현재 타일 상태 스냅샷** 기준으로 계산하지만, 실제 이동은 물리 시간이 필요

- 0.1초 간격 재계산 사이에 다른 유닛의 이동으로 타일 상태가 바뀌면 계산된 경로가 무효화됨

- 이를 `ResolveMoveRequests`에서 즉각 처리하지만, 대체 이동이 이미 경로를 흐트러뜨림