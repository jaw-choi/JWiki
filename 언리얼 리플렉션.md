Property System in Unreal engine

언리얼 리플렉션/프로퍼티 시스템은 C++에 없는 런타임 자기정보(타입, 멤버, 함수, 메타데이터)를 엔진이 쓰기 위해, 빌드 과정에서 별도 툴이 헤더를 스캔해 “반사 정보”를 생성하고 런타임에 조회 가능하게 만드는 구조입니다.

1. 전체 파이프라인
    

- 헤더에 UCLASS / USTRUCT / UENUM / UFUNCTION / UPROPERTY 같은 매크로로 “리플렉션 대상”을 표시
    
- UHT(Unreal Header Tool)가 해당 헤더를 파싱해서 .generated.h / .gen.cpp 형태의 등록 코드를 자동 생성
    
- 컴파일 시 생성된 코드가 모듈에 포함되고, 런타임에 UClass, UScriptStruct, UFunction, FProperty 같은 메타 오브젝트로 등록됨
    
- 결과적으로 에디터(디테일 패널), 블루프린트, 직렬화, GC, 네트워크 복제 같은 시스템이 공통 메타데이터를 기반으로 동작
    

2. 프로퍼티 시스템이 하는 일(UPROPERTY가 붙은 멤버 변수)  
    UPROPERTY를 붙이면 그 멤버가 “엔진이 아는 필드”가 됩니다. 그 필드는 보통 FProperty 계열 메타로 표현되며,
    

- 타입 정보(예: int32, float, UObject*, TArray 등)
    
- 메모리 오프셋(객체 메모리 안에서 어디에 있는지)
    
- 플래그(복제, 저장, 가비지 컬렉션 추적, 편집기 노출, 블루프린트 노출 등)
    
- 메타데이터(표시 이름, 편집 조건, 카테고리, 툴팁 등)  
    를 갖습니다.
    

3. 왜 UPROPERTY가 GC/직렬화/에디터에 직접 연결되나
    

- 언리얼은 UObject 포인터를 임의로 추적할 수 없으니, UPROPERTY로 표시된 참조를 통해 GC가 “살아있는 참조 그래프”를 따라갑니다.
    
- 직렬화도 동일하게, 리플렉션 메타를 순회하면서 저장/로드 규칙을 적용합니다.
    
- 디테일 패널, 블루프린트 노출도 같은 메타데이터를 재사용합니다.
    

4. UPROPERTY 지정자(specifier)와 metadata의 차이
    

- specifier는 엔진 동작에 영향을 주는 “플래그” 성격(예: EditAnywhere, BlueprintReadWrite, Replicated 등)
    
- meta=는 주로 에디터/툴링/표시 방식에 대한 “추가 힌트”(예: ClampMin, ToolTip, EditCondition 등)  
    공식 문서가 타입별로 지원 목록을 정리해 둡니다.
    

5. UProperty vs FProperty (타입 이름 혼동 포인트)
    

- UPROPERTY는 매크로(표기)
    
- 과거 메타 타입이 UProperty 계열이었는데, UE4.25 전후로 FProperty 계열로 이동한 흐름이 있습니다.  
    즉 “UPROPERTY가 없어졌다”가 아니라, 리플렉션 필드 메타를 표현하는 C++ 타입이 바뀐 쪽에 가깝습니다.
    

6. 최소 예시(프로퍼티가 엔진 시스템에 연결되는 느낌)
    

`UCLASS() class AMyActor : public AActor {     GENERATED_BODY()  public:     // 에디터에서 수정 가능 + 블루프린트에서 읽기/쓰기 가능     UPROPERTY(EditAnywhere, BlueprintReadWrite, Category="Stats")     int32 HP = 100;      // GC가 추적할 UObject 참조     UPROPERTY(VisibleAnywhere, Category="Refs")     TObjectPtr<UStaticMeshComponent> Mesh;      // 네트워크 복제 예시(추가로 GetLifetimeReplicatedProps 구현 필요)     UPROPERTY(ReplicatedUsing=OnRep_Level, Category="Net")     int32 Level = 1;      UFUNCTION()     void OnRep_Level(); };`

원하시면 지금 수준에서 가장 빠르게 이해되는 방식으로,

- .generated.h / .gen.cpp에 실제로 어떤 등록 코드가 생기는지
    
- 런타임에서 UClass와 FProperty를 순회하며 값을 읽는 방법(오프셋 기반 접근)
    
- dynamic_cast를 대체하는 IsA/Cast 계열이 왜 이 시스템 위에서 동작하는지  
    이 3가지를 한 흐름으로 묶어서 정리해드릴 수 있습니다.