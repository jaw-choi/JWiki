# [[Daily]] 2026-01-26



### 오늘의 코딩 문제
> 백준1377 버블 소트 패스 계산 - 골드2
> 답지 보고 품 - 다시 풀어보기
버블소트의 특성, 패스가 한번 진행되면 어떤 값의 왼쪽으로 이동 수가 최대 1번이기 때문에,
기존의 인덱스와 정렬된 이후의 인덱스를 비교하여 왼쪽으로 이동 수가 가장 큰 값이 몇 번인지에 따라 패스 수가 정해짐
버블 소트의 특성을 이해하고 있나

### 면접 준비

*복잡한 까다로운 질문에 대해서는 소리내어 말하는 연습을 하라*

[업캐스팅 - 형변환이 안전한다는것을 메모리적으로 설명하시오]
	-> 메모리영역 cpu 
	- 업캐스팅 ->클래스 상속 포인터 주소 연산 - >시작점부터 얼만큼의 공간을 읽을지,허용받았는지 컴퓨터는 멍청해서 주소밖에 모름, 자손입장에서 부모공간을 짜부시켜도 항상 포함 따라서 안전함
	- 다운캐스팅 -> 부모가 변수가지고 있고 child가 추가적 변수를 가지고있을때 부모가 자식ㅡ로 형변환 가능할려면 추가된 변수크기만큼허용되는데 그러지않기때문에 위험합니다
	- 되는 경우 -> 자식클래스가 하나도 추가하지 않았을때
				- 부모가 자식으로 치환했을때 ->(E\* e = new child()) 시작주소를 받음, E\*로 받으면 부모의 저장공간까지만 씀
	형변환이 무엇이냐(업은?다운은?) =>메모리적으로 설명가능하기
	
	업캐스팅:
	부모
	↑
	자식
	
	다운캐스팅:
	부모
	↓
	자식
	
[push_back과 emplace back 차이]
        //push_back lvalue ref 받음-> 복사
        //emplace_back rvalue ref 고려한 함수 -> 임시값으로 보고 복사x
        요즘엔 똑같음- pushback도 rvalue ref버전 있음
        
[downcasting때 왜 dynamic_cast쓰나요]
안정성을 위하여 dynamic_cast를 쓰면 casting 실패시 nullptr을 반환하기 때문에 안정적으로 검사 할 수 있다.

## 이해가 부족한 부분
[[SOLID]] -> D(Dependenct inversion principle - .의존성 역전 원칙)
	-> Dependenct inversion,Dependenct injection이 뭔지 보기
	->class는 저수준 모듈(구현된 객체)보다 고수준 모듈(인터페이스같은 추상적 개념)에 의존해야한다
	->main class에서 다른 객체에 직접 의존한다면 -> 추가되면 또 직접 추가해야함.
	->추상적개념을 통해 객체사용
	->새로운 알림방식 추가되어도 main class는 변경 없음
## 메모

[auto]  런타임에 영향 주냐? -> x
		-> 컴파일 시점에 정해지기 때문에
		
[extern] 키워드 쓰면 전역변수 사용 가능

- unreal은 this 안씀

## [[CS - 상속]]

## [[CS - 캐스팅]]
Entity* player = ==(Entity*)==new Player("Jae");
==메모리는 Player 전체로 존재하지만, 포인터 타입이 Entity\*라서 ‘보이는 인터페이스’가 Entity로 제한된다.== ->(Entity\*)가 숨겨져 있기 때문



## [[shared_ptr + dynamic_cast 탐색에서 ref_count atomic 부담 줄이기]]
업캐스팅 되었을때->메모리시작부터어디까지 쓸수있는 권한을 얻는다

## [[CS - Virtual]]
