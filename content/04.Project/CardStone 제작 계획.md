# 7일 과제 전형 기획 정리

## 1. 현재 과제 분석

| 항목 | 내용 |
|---|---|
| 장르 | 세로형 턴제 카드 배틀 |
| 기본 구조 | 각 플레이어 카드 6장 보유 |
| 전장 구조 | 전장 3장 + 대기 3장 |
| 카드 타입 | 일반 / 원거리 / 무쌍 / 힐러 |
| 핵심 메카닉 | HP = 공격력 |
| 전투 방식 | 카드 선택 → 대상 선택 → 공격/스킬 처리 |
| AI | 상대 턴 자동 진행 |
| 승리 조건 | 상대 카드 전부 제거 |
| 제출 목표 | Android APK + README + 플레이 영상 + 개발 일지 |

---

## 2. 내가 느낀 문제점

처음에는 이 게임이 단순한 턴제 RPG가 아니라,

> **덱 자체가 체력이고, 자원이고, 패배 조건이 되는 게임**

처럼 느껴졌다.

즉, 이런 구조를 기대했다.

```text
덱 = 체력
덱 = 자원
덱 = 전략의 중심
덱이 소모되면 패배

이 구조라면 굉장히 재미있는 전략이 나올 수 있다.

예를 들면,

- 어떤 덱은 자기 덱에 카드를 계속 넣어서 덱을 불린다.
- 어떤 덱은 상대 덱을 빠르게 소모시킨다.
- 어떤 덱은 덱을 계속 순환시키면서 오래 버틴다.
- 어떤 덱은 덱을 줄이는 대신 순간 화력이 강하다.
- 어떤 덱은 전장 카드보다 대기 덱을 직접 공격한다.
```text
```
이렇게 되면 단순히 유닛을 때려잡는 게임이 아니라,

> **덱을 어떻게 유지하고, 소모시키고, 순환시키느냐가 핵심인 카드 게임**

이 된다.

하지만 실제 과제 구조를 보면 현재 기본안은 카드 게임이라기보다는,

> **카드 게임의 외형을 가진 턴제 RPG 전투**

에 가깝다.

내 유닛 3장, 상대 유닛 3장이 있고, 각 유닛이 스킬을 사용해서 상대를 공격한다.  
전장 카드가 죽으면 대기 카드가 올라오고, 모든 카드가 죽으면 패배한다.

이 구조는 결국,

```
3:3 턴제 RPG파이널 판타지식 전투포켓몬식 파티 전투용병단 데스매치
```

에 가깝다.

그래서 그대로 구현하면 게임성이 너무 단순해질 수 있다.

---

## 3. 개선 방향

기본 과제 요구사항은 유지한다.

```
카드 6장전장 3장대기 3장턴제 전투카드 선택대상 선택공격/스킬 처리카드 제거자동 배치승패 판정AI 턴
```

다만 해석을 바꾼다.

기존 해석:

> 카드가 죽었다.

개선 해석:

> 덱 체력이 깎였다.

즉, 실제 승리 조건은 기존과 동일하게 **상대 모든 카드 제거**이지만, 플레이어가 느끼는 감각은

> **덱 전체가 하나의 생명력이다.**

가 되도록 만든다.

---

# Deck Core Battle

## 4. 개선 기획 콘셉트

### 제목

**Deck Core Battle**

### 한 줄 설명

> 6장의 카드로 구성된 덱을 운용하며, 전장 카드와 대기 카드를 관리해 상대의 덱 코어를 먼저 붕괴시키는 세로형 턴제 카드 배틀 게임.

### 핵심 콘셉트

```
카드 = 유닛덱 = 체력대기 카드 = 예비 병력 + 자원카드 제거 = 덱 코어 감소모든 카드 제거 = 덱 코어 붕괴
```

---

## 5. 핵심 재미

이 게임의 핵심 재미는 단순히 상대를 공격하는 것이 아니라,

> **지금 공격할 것인가, 내 덱을 보존할 것인가**

를 선택하는 데서 나온다.

예를 들어 체력이 높은 카드는 공격력이 높기 때문에 강하다.  
하지만 공격 후 반격을 크게 받으면 다음 턴에 제거될 수 있다.

반대로 체력이 낮은 카드를 전장에서 빼고 대기 덱으로 순환시키면 생존력은 높아진다.  
하지만 그 턴의 공격 기회는 줄어든다.

즉, 플레이어는 매 턴 이런 고민을 하게 된다.

```
지금 죽일까?내 카드를 살릴까?상대 대기 덱을 갉아먹을까?힐러로 버틸까?순환으로 전장을 바꿀까?
```

---

## 6. 기존 과제와 차별화되는 부분

|구분|기존 구조|개선 구조|
|---|---|---|
|카드 제거|유닛 하나 사망|덱 체력 감소|
|대기 카드|죽으면 올라오는 예비 카드|덱 자원|
|전투 목표|상대 카드 공격|상대 덱 코어 붕괴|
|전략성|전장 3장 중심|전장 + 대기 덱 관리|
|재미 방향|턴제 RPG|덱 관리형 카드 배틀|

---

## 7. 게임 분석 요약

|항목|내용|
|---|---|
|장르|세로형 턴제 카드 배틀|
|구조|각 6장, 전장 3장 + 대기 3장|
|기본 카드 타입|일반 / 원거리 / 무쌍 / 힐러|
|추가 카드 타입|순환자 / 소모술사 / 증원병|
|핵심 메카닉|HP = 공격력|
|개선 메카닉|덱 코어 = 남은 카드 수|
|핵심 감각|카드가 죽는 것이 아니라 덱 생명력이 깎이는 느낌|
|AI|상대 턴 자동 진행|
|승리 조건|상대 덱 코어 0, 즉 상대 카드 전부 제거|

---

# 시스템 설계

## 8. 덱 코어 시스템

각 플레이어는 총 6장의 카드를 가진다.

```
전장 카드 3장대기 카드 3장총 덱 코어 6
```

카드가 제거될 때마다 덱 코어가 1 감소한다.

```
DeckCore = 전장 생존 카드 수 + 대기 생존 카드 수
```

예시:

```
시작 상태전장 3장 + 대기 3장 = 덱 코어 6카드 1장 제거전장 2장 + 대기 3장 = 덱 코어 5대기 카드 자동 배치전장 3장 + 대기 2장 = 덱 코어 5
```

중요한 점은 별도의 본체 HP를 만들지 않는다는 것이다.  
남은 카드 수 자체가 체력처럼 작동한다.

---

## 9. HP = 공격력 시스템

이 게임의 기본 전투 규칙은 다음과 같다.

```
카드의 현재 HP = 카드의 공격력
```

즉, 체력이 높은 카드는 강한 피해를 준다.

예시:

```
HP 7 카드가 공격하면 7 피해HP 3 카드가 공격하면 3 피해
```

이 구조의 장점은 직관성이다.

플레이어는 별도의 공격력 수치를 외울 필요 없이,

> 체력이 높을수록 강하다.

라고 이해하면 된다.

하지만 동시에 딜레마가 생긴다.

체력이 높은 카드는 강하지만, 반격을 많이 받으면 공격력이 급격히 낮아진다.  
따라서 무작정 강한 카드로 때리는 것이 항상 정답은 아니다.

---

## 10. 기본 카드 타입

|카드 타입|역할|특징|
|---|---|---|
|일반|기본 근접 공격|공격 후 반격을 받음|
|원거리|안전한 단일 공격|반격을 받지 않음|
|무쌍|광역 공격|대상 인접 카드에도 추가 피해|
|힐러|유지력 담당|턴 시작 시 아군 HP 회복|

---

## 11. 추가 카드 타입

### 11-1. 순환자

|항목|내용|
|---|---|
|역할|덱 순환|
|효과|아군 전장 카드 1장을 대기 덱으로 보내고, 대기 덱 맨 앞 카드를 전장에 배치|
|목적|죽을 것 같은 카드를 보존하고 전장 구성을 바꿈|
|재미|전장 3장 고정 싸움이 아니라 덱을 굴리는 느낌 제공|
|구현 난이도|중간|

#### 예시

```
체력 1 남은 일반 카드가 전장에 있음순환자 효과 사용체력 1 일반 카드를 대기 덱으로 이동대기 덱의 새 카드가 전장에 등장
```

---

### 11-2. 소모술사

|항목|내용|
|---|---|
|역할|상대 덱 견제|
|효과|적 전장 카드에 피해 + 상대 대기 카드 1장에 약한 피해|
|목적|전장뿐 아니라 상대 덱 전체를 갉아먹음|
|재미|상대 대기 덱을 직접 압박하는 전략 제공|
|구현 난이도|낮음|

#### 예시

```
소모술사가 적 전장 카드에게 2 피해상대 대기 덱 맨 위 카드에게 1 피해
```

---

### 11-3. 증원병

|항목|내용|
|---|---|
|역할|덱 보존|
|효과|아군 대기 카드 1장의 HP 회복|
|대기 카드가 없을 경우|자신을 회복|
|목적|전장 밖 자원을 관리하는 재미 제공|
|구현 난이도|낮음|

#### 예시

```
대기 카드 중 체력이 낮은 카드 1장 회복대기 카드가 없다면 증원병 자신의 HP 회복
```

---

# 전투 흐름

## 12. 기본 전투 루프

게임 시작
↓
양쪽 카드 6장 생성
↓
전장에 3장 배치
↓
대기 덱에 3장 보관
↓
플레이어 턴 시작
↓
카드 선택
↓
행동 선택
↓
대상 선택
↓
효과 적용
↓
카드 제거 확인
↓
대기 카드 자동 배치
↓
승패 판정
↓
적 턴 시작
↓
AI 행동
↓
반복

---

## 13. 상태 머신 설계

```
[PlayerSelectCard]
    ↓ 카드 선택

[PlayerSelectAction]
    ↓ 공격 or 스킬 선택

[PlayerSelectTarget]
    ↓ 타겟 선택

[ApplyEffect]
    ↓ HP 계산
    ↓ 카드 제거
    ↓ 자동 배치
    ↓ 승패 판정

[EnemyTurn]
    ↓ AI 행동
    ↓ ApplyEffect

[PlayerSelectCard]
    ↓ 반복
```

---

## 14. 상태 설명

|상태|설명|
|---|---|
|PlayerSelectCard|플레이어가 행동할 카드를 선택하는 상태|
|PlayerSelectAction|공격 또는 스킬을 선택하는 상태|
|PlayerSelectTarget|효과를 적용할 대상을 선택하는 상태|
|ApplyEffect|피해, 회복, 제거, 자동 배치, 승패 판정을 처리하는 상태|
|EnemyTurn|AI가 행동을 결정하고 실행하는 상태|
|GameOver|승패가 결정된 상태|

---

# 재미 요소 설계 포인트

## 15. 면접 답변용 핵심 포인트

### 15-1. HP = 공격력 딜레마

이 게임은 HP가 곧 공격력이다.

따라서 체력이 높은 카드는 강력한 피해를 줄 수 있지만, 반격을 받으면 다음 공격력이 줄어든다.

즉, 플레이어는 단순히 강한 카드로 공격하는 것이 아니라,

> 이 카드를 지금 공격에 써도 되는가?

를 고민하게 된다.

---

### 15-2. 덱 코어 긴장감

카드가 죽는 것은 단순한 유닛 사망이 아니라 덱 코어 감소다.

전장 카드가 죽으면 대기 카드가 올라오지만, 대기 카드가 줄어들수록 패배에 가까워진다.

따라서 플레이어는 매 턴 남은 카드 수를 의식하게 된다.

---

### 15-3. 정보 비대칭

상대 대기 카드는 뒤집혀 있다.

그래서 플레이어는 다음에 어떤 카드가 등장할지 알 수 없다.

이로 인해 단순 계산 싸움이 아니라 약간의 긴장감과 변수가 생긴다.

---

### 15-4. 무쌍의 위치 전략

무쌍 카드는 대상뿐 아니라 인접 카드에도 피해를 준다.

따라서 슬롯 배치와 공격 대상 선택이 중요해진다.

예를 들어 중앙 슬롯의 카드를 공격하면 좌우 카드까지 피해를 줄 수 있다.

---

### 15-5. 힐러 시너지

힐러는 직접 전투력은 낮지만 아군의 HP를 회복시킨다.

이 게임에서는 HP가 공격력이기 때문에, 힐러는 단순 생존 보조가 아니라 공격력 유지 역할도 한다.

---

### 15-6. 순환자 전략

순환자는 죽기 직전의 카드를 대기 덱으로 돌려보낼 수 있다.

이를 통해 카드 제거를 늦추고, 덱 코어를 보존할 수 있다.

---

### 15-7. 소모술사 전략

소모술사는 상대 전장 카드뿐 아니라 대기 카드도 공격한다.

이를 통해 상대의 미래 전력을 미리 약화시킬 수 있다.

---

# 아키텍처 설계

## 16. 폴더 구조

Assets/
├── _Scripts/
│   ├── Core/
│   │   ├── GameManager.cs
│   │   └── BattleStateMachine.cs
│   │
│   ├── Card/
│   │   ├── SO_CardData.cs
│   │   ├── CardController.cs
│   │   ├── CardEffectHandler.cs
│   │   ├── CardType.cs
│   │   └── CardRuntimeData.cs
│   │
│   ├── Battle/
│   │   ├── BattleManager.cs
│   │   ├── TurnManager.cs
│   │   ├── FieldManager.cs
│   │   ├── BattleResolver.cs
│   │   └── EnemyAI.cs
│   │
│   └── UI/
│       ├── BattleUI.cs
│       ├── CardUI.cs
│       ├── CardSlotUI.cs
│       ├── DeckCoreUI.cs
│       └── ResultUI.cs
│
├── _Prefabs/
│   ├── PF_Card.prefab
│   └── PF_CardSlot.prefab
│
├── _ScriptableObjects/
│   └── Cards/
│       ├── SO_Normal.asset
│       ├── SO_Ranged.asset
│       ├── SO_Warlord.asset
│       ├── SO_Healer.asset
│       ├── SO_Rotator.asset
│       ├── SO_Exhauster.asset
│       └── SO_Reinforcer.asset
│
├── _Sprites/
│   ├── Cards/
│   └── UI/
│
├── _Scenes/
│   ├── SC_Title.unity
│   └── SC_Battle.unity
│
└── DevLog/
    ├── Day1.md
    ├── Day2.md
    ├── Day3.md
    ├── Day4.md
    ├── Day5.md
    ├── Day6.md
    └── Day7.md
```

---

## 17. 주요 클래스 역할

|클래스|역할|
|---|---|
|GameManager|씬 전환, 전체 게임 상태 관리|
|BattleStateMachine|전투 상태 흐름 제어|
|SO_CardData|카드 고정 데이터 관리|
|CardRuntimeData|전투 중 변하는 카드 데이터 관리|
|CardController|카드 인스턴스 제어|
|CardEffectHandler|카드 타입별 효과 처리|
|BattleManager|전투 로직 총괄|
|TurnManager|플레이어/적 턴 전환|
|FieldManager|전장 슬롯, 대기 덱, 자동 배치 관리|
|BattleResolver|피해 계산, 회복, 사망 처리|
|EnemyAI|적 행동 결정|
|BattleUI|전투 화면 전체 UI 관리|
|CardUI|카드 HP, 타입, 선택 표시|
|DeckCoreUI|덱 코어 표시|
|ResultUI|승패 결과 화면|

---

## 18. 네이밍 규칙

|종류|규칙|예시|
|---|---|---|
|Script|PascalCase|BattleManager.cs|
|ScriptableObject|SO_ 접두사|SO_CardData.cs|
|Prefab|PF_ 접두사|PF_Card.prefab|
|Scene|SC_ 접두사|SC_Battle.unity|
|Interface|I 접두사|ICardEffect.cs|
|Enum|PascalCase|CardType, TurnState|
|Private Field|camelCase|currentHp|
|Public Property|PascalCase|CurrentHp|
|Method|PascalCase|ExecuteAttack|

---

## 19. 주석 스타일

```
/// <summary>/// [역할] 카드가 공격 행동을 실행한다./// [설계 이유] 카드 타입별 효과 분기를 BattleManager가 아닌 CardEffectHandler에 위임하여/// 새로운 카드 타입 추가 시 기존 전투 흐름을 크게 수정하지 않도록 설계했다./// </summary>public void ExecuteAttack(CardController attacker, CardController target){}
```

---

# 데이터 설계

## 20. CardType enum

```
public enum CardType{    Normal,    Ranged,    Warlord,    Healer,    Rotator,    Exhauster,    Reinforcer}
```

---

## 21. TurnState enum

```
public enum TurnState{    PlayerSelectCard,    PlayerSelectAction,    PlayerSelectTarget,    ApplyEffect,    EnemyTurn,    GameOver}
```

---

## 22. SO_CardData 설계

```
using UnityEngine;[CreateAssetMenu(fileName = "SO_CardData", menuName = "Card/Card Data")]public class SO_CardData : ScriptableObject{    public string cardId;    public string cardName;    public CardType cardType;    public int maxHp;    public Sprite cardSprite;    [TextArea]    public string description;}
```

---

## 23. CardRuntimeData 설계

```
public class CardRuntimeData{    public SO_CardData CardData { get; private set; }    public int CurrentHp { get; private set; }    public bool IsAlive => CurrentHp > 0;    public int AttackPower => CurrentHp;    public CardRuntimeData(SO_CardData cardData)    {        CardData = cardData;        CurrentHp = cardData.maxHp;    }    public void TakeDamage(int damage)    {        CurrentHp -= damage;        if (CurrentHp < 0)        {            CurrentHp = 0;        }    }    public void Heal(int amount)    {        CurrentHp += amount;        if (CurrentHp > CardData.maxHp)        {            CurrentHp = CardData.maxHp;        }    }}
```

---

# 전투 규칙

## 24. 일반 카드

```
대상에게 자신의 현재 HP만큼 피해를 준다.대상도 살아있다면 반격한다.반격 피해량은 대상의 현재 HP 기준이다.
```

---

## 25. 원거리 카드

```
대상에게 자신의 현재 HP만큼 피해를 준다.반격을 받지 않는다.
```

---

## 26. 무쌍 카드

```
대상에게 자신의 현재 HP만큼 피해를 준다.대상 좌우 인접 카드에게 추가 피해를 준다.
```

---

## 27. 힐러 카드

```
턴 시작 시 아군 전장 카드 중 HP가 가장 낮은 카드를 회복한다.직접 공격은 일반 공격과 동일하게 처리한다.
```

---

## 28. 순환자 카드

```
아군 전장 카드 1장을 선택한다.선택한 카드를 대기 덱 맨 뒤로 보낸다.대기 덱 맨 앞 카드를 해당 슬롯에 배치한다.
```

---

## 29. 소모술사 카드

```
대상 적 전장 카드에게 피해를 준다.상대 대기 덱 맨 앞 카드에게 1 피해를 준다.
```

---

## 30. 증원병 카드

```
아군 대기 덱 카드 중 HP가 가장 낮은 카드를 회복한다.대기 덱이 없다면 자신을 회복한다.
```

---

# AI 설계

## 31. EnemyAI 기본 정책

7일 과제에서는 복잡한 AI보다 명확한 우선순위 기반 AI가 적합하다.

```
1순위: 이번 공격으로 죽일 수 있는 카드가 있으면 공격2순위: HP가 가장 낮은 적 카드 공격3순위: 무쌍 카드가 있으면 중앙 대상 우선 공격4순위: 소모술사는 대기 카드가 많은 상대에게 사용5순위: 그 외에는 랜덤 공격
```

---

## 32. AI 설계 이유

AI는 완벽한 판단보다 다음 조건을 만족하는 것이 중요하다.

```
턴이 자동으로 진행된다.공격 대상이 어색하지 않다.플레이어가 예측 가능한 규칙을 느낄 수 있다.게임이 처음부터 끝까지 완주된다.
```

---

# UI 설계

## 33. 전투 화면 구성

```
상단- 적 덱 코어- 적 전장 카드 3장- 적 대기 카드 3장중앙- 턴 표시- 전투 로그하단- 내 전장 카드 3장- 내 대기 카드 3장- 내 덱 코어- 행동 버튼
```

---

## 34. UI 표시 요소

|UI|내용|
|---|---|
|카드 HP|현재 HP 표시|
|카드 타입|일반/원거리/무쌍/힐러/추가 타입 표시|
|선택 표시|선택 가능한 카드 하이라이트|
|대상 표시|공격 가능한 대상 하이라이트|
|덱 코어|남은 카드 수 표시|
|턴 표시|Player Turn / Enemy Turn|
|결과 화면|Victory / Defeat|

---

# 7일 개발 일정

## 35. 전체 일정

|일차|날짜|목표|검증 기준|
|---|---|---|---|
|Day 1|6/13|프로젝트 구조 + SO_CardData + CardType enum + BattleStateMachine 뼈대|Inspector에서 카드 데이터 편집 가능|
|Day 2|6/14|FieldManager 구현. 슬롯 3개 + 대기 카드 + 자동 배치 로직|시작 시 양쪽 3장 배치, 제거 시 자동 보충|
|Day 3|6/15|TurnManager + 4가지 기본 카드 효과 + HP 계산 + 힐러 턴 시작 효과|각 카드 타입 전투가 규칙대로 동작|
|Day 4|6/16|EnemyAI + 승패 판정 + 전체 흐름 완성|처음부터 끝까지 한 판 완주 가능|
|Day 5|6/17|UI 전체 연결. 카드 HP, 턴 표시, 선택 하이라이트, 결과 화면|UI가 게임 상태와 실시간 동기화|
|Day 6|6/18|공격/피격 FX + 카드 일러스트 + 가산점 카드 추가 + 버그픽스|플레이테스트 통과|
|Day 7|6/19|Android APK 빌드 + README + YouTube 영상 + 개발 일지 정리 + 제출|APK 실기기 실행 확인|

---

# Day 1 작업 정리

## 36. 오늘 바로 할 작업

```
1. Unity 프로젝트 생성2. 폴더 구조 생성3. Git 초기화4. SO_CardData.cs 작성5. CardType enum 작성6. TurnState enum 작성7. BattleStateMachine.cs 뼈대 작성8. DevLog/Day1.md 작성
```

---

## 37. Day 1 완료 기준

```
Unity 프로젝트가 정상 실행된다.Assets 폴더 구조가 정리되어 있다.SO_CardData 에셋을 생성할 수 있다.Inspector에서 카드 이름, 타입, HP를 입력할 수 있다.BattleStateMachine에서 현재 상태를 로그로 확인할 수 있다.Day1.md 개발 일지가 작성되어 있다.
```

---

## 38. Day 1 개발 일지 초안

```
# Day 1 개발 일지## 오늘 목표- 프로젝트 기본 구조 생성- 카드 데이터 구조 설계- 전투 상태 머신 뼈대 작성## 작업 내용- Unity 프로젝트 생성- Assets 폴더 구조 정리- SO_CardData ScriptableObject 작성- CardType enum 작성- TurnState enum 작성- BattleStateMachine 클래스 작성## 설계 의도이번 과제는 7일 안에 플레이 가능한 턴제 카드 배틀 프로토타입을 완성하는 것이 목표다.기본 과제 구조는 3장의 전장 카드와 3장의 대기 카드로 구성되어 있다.  단순히 카드 HP를 깎는 턴제 RPG처럼 보이지 않도록, 덱 전체를 하나의 체력 자원으로 느끼게 만드는 방향으로 기획을 확장했다.이를 위해 카드 제거를 단순한 유닛 사망이 아니라 덱 코어 감소로 표현하고, 이후 순환자/소모술사 같은 추가 카드 타입을 통해 전장과 대기 덱을 함께 관리하는 재미를 만들 계획이다.## 내일 작업- FieldManager 구현- 전장 슬롯 3개 구성- 대기 덱 구성- 시작 시 카드 자동 배치- 카드 제거 시 대기 카드 자동 보충
```

---

# README에 넣을 핵심 설명

## 39. 프로젝트 소개

```
# Deck Core BattleDeck Core Battle은 6장의 카드로 구성된 덱을 운용해 상대의 덱 코어를 먼저 붕괴시키는 세로형 턴제 카드 배틀 게임입니다.기본 규칙은 3장의 전장 카드와 3장의 대기 카드로 구성됩니다.  전장 카드가 제거되면 대기 카드가 자동으로 전장에 배치되며, 모든 카드가 제거되면 패배합니다.본 프로젝트는 단순한 턴제 RPG 전투가 아니라, 카드 제거를 덱 코어 감소로 해석하여 덱 전체를 하나의 생명력 자원처럼 느끼도록 설계했습니다.
```

---

## 40. 주요 특징

```
## 주요 특징- 3장의 전장 카드와 3장의 대기 카드로 구성된 턴제 카드 배틀- HP가 곧 공격력이 되는 직관적인 전투 규칙- 카드 제거가 덱 코어 감소로 이어지는 덱 생명력 시스템- 일반, 원거리, 무쌍, 힐러 카드 타입 구현- 순환자, 소모술사, 증원병 등 추가 카드 타입 확장 가능 구조- 간단한 AI 턴 자동 진행- Android APK 빌드 지원
```

---

# 면접 답변 정리

## 41. 이 기획을 선택한 이유

```
기본 과제 구조를 분석했을 때, 단순히 전장 카드끼리 HP를 깎는 방식으로 구현하면 카드 게임보다는 턴제 RPG에 가까운 경험이 될 수 있다고 판단했습니다.그래서 과제의 기본 규칙은 유지하되, 카드 6장 전체를 하나의 생명력 자원처럼 느끼도록 덱 코어 시스템을 추가했습니다.이를 통해 플레이어는 단순히 지금 전장의 적을 공격하는 것이 아니라, 내 카드를 보존할지, 상대 대기 덱을 압박할지, 전장 카드를 순환시킬지 고민하게 됩니다.
```

---

## 42. 가장 중요하게 본 재미 요소

```
가장 중요하게 본 재미 요소는 HP가 공격력이라는 규칙에서 발생하는 딜레마입니다.체력이 높은 카드는 강한 피해를 줄 수 있지만, 반격을 받으면 체력이 줄고 다음 공격력도 약해집니다.따라서 플레이어는 매 턴 가장 강한 공격을 할지, 카드를 보존할지 선택해야 합니다.여기에 덱 코어 시스템을 더해 카드 하나의 죽음이 전체 생존 자원 감소로 느껴지게 만들었습니다.
```

---

## 43. 코드 구조에서 신경 쓴 부분

```
카드 데이터와 전투 중 상태를 분리했습니다.SO_CardData는 카드 이름, 타입, 최대 HP 같은 고정 데이터를 관리하고, CardRuntimeData는 현재 HP처럼 전투 중 변하는 값을 관리합니다.또한 카드 타입별 효과는 CardEffectHandler에서 처리하도록 분리했습니다.이렇게 하면 새로운 카드 타입을 추가할 때 BattleManager 전체를 수정하지 않고 효과 처리 부분만 확장할 수 있습니다.
```

---

## 44. 7일 안에 완성하기 위한 범위 조절

```
처음부터 덱 빌딩, 카드 획득, 성장 시스템까지 구현하면 7일 안에 완성도가 떨어질 수 있다고 판단했습니다.그래서 이번 과제에서는 한 판의 전투 경험을 완성하는 데 집중했습니다.필수 기능인 턴 진행, 카드 선택, 대상 선택, 공격 처리, 카드 제거, 자동 배치, 승패 판정, AI 턴을 먼저 완성하고, 이후 여유가 생기면 순환자나 소모술사 같은 추가 카드 타입을 넣는 방식으로 범위를 조절했습니다.
```

---

# 최종 방향 요약

## 45. 한 문장 요약

> 기본 과제의 3:3 턴제 카드 배틀 구조는 유지하되, 카드 6장 전체를 하나의 생명력 자원으로 해석해 덱을 보존하고 순환시키는 재미를 강화한다.

---

## 46. 개발 우선순위

```
1순위: 기본 턴제 전투 완성2순위: 카드 제거 + 자동 배치 + 승패 판정3순위: UI와 AI 연결4순위: 덱 코어 UI 추가5순위: 순환자/소모술사 추가6순위: FX, 사운드, 카드 일러스트7순위: README, 영상, APK 제출
```

---

## 47. 절대 하지 말아야 할 것

```
덱 빌딩 시스템까지 욕심내지 않기카드 수를 너무 많이 늘리지 않기복잡한 AI 만들지 않기연출보다 전투 완주를 먼저 만들기UI 꾸미기보다 상태 동기화를 먼저 하기새 카드보다 기본 4종 카드부터 완성하기
```

---

## 48. 최종 제출 목표

```
Android APK 실행 가능처음부터 끝까지 한 판 플레이 가능플레이어 카드 선택 가능대상 선택 가능AI 턴 자동 진행카드 제거 시 자동 배치 가능승패 결과 화면 출력README 작성개발 일지 작성YouTube 플레이 영상 제출
```