
   * 핵심 목표: (총알 수 * 적 수)에 달하는 무식한 충돌 검사(Brute-force)를 피해 효율적으로 충돌을 판정한다.
   * 구현 전략:
       1. AABB(Axis-Aligned Bounding Box) 구현: 모든 Actor가 자신의 위치와 크기를 기반으로 한 사각형 충돌 영역(Rect 또는 AABB 구조체)을 가지게 합니다.
       2. `Quadtree` 클래스 설계: 맵 전체를 포괄하는 하나의 루트 노드(Root Node)로 시작하는 쿼드트리 자료구조를 만듭니다.
       3. 매 프레임 재구성:
           * 매 프레임 시작 시, 쿼드트리를 Clear합니다.
           * Level에 있는 모든 active 상태의 객체(플레이어, 적, 총알, 아이템 등)를 쿼드트리에 Insert합니다. 객체가 특정 구역에 몰리면 해당 노드는 4개의 자식
             노드로 분할됩니다.
       4. 충돌 검사: 특정 객체(예: 플레이어 총알)의 충돌을 검사할 때, 맵 상의 모든 적이 아닌, 쿼드트리에서 "해당 총알이 속한 노드 및 인접 노드"에 있는
          적들만 후보로 가져와 AABB 검사를 수행합니다.
   * 위기와 해결:
       * 위기(Problem): 객체가 500개만 되어도, 프레임당 충돌 연산 횟수가 500 * 500 = 250,000 번에 육박하며 연산량이 기하급수적으로 증가해 게임 진행이
         불가능해진다.
       * 해결(Solution): 쿼드트리를 사용하여 검사 대상을 "내 주변의 객체"로 한정시켜, 충돌 연산 복잡도를 O(N²)에서 O(N log N) 수준으로 획기적으로 낮춥니다.