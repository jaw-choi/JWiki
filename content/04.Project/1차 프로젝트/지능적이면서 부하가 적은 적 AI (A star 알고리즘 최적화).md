
   * 핵심 목표: 수많은 적들이 플레이어를 향해 길을 찾아오되, 매 프레임 모든 적에 대해 A* 알고리즘을 계산해서 발생하는 프레임 드랍을 막는다.
   * 구현 전략:
       1. `PathfindingManager` 설계: A* 길찾기 요청을 처리하는 전역 관리자를 만듭니다.
       2. 시간 분산 처리 (Time Slicing): 말씀하신 대로, A* 계산을 여러 프레임에 걸쳐 분산시킵니다.
           * PathfindingManager는 "길찾기 요청 큐"를 가집니다.
           * 적(Enemy)은 자신의 경로가 없거나, 현재 경로에서 일정 거리 이상 벗어났을 때 매니저에게 길찾기를 '요청'하고 큐에 등록합니다.
           * 매니저는 매 프레임, 큐에서 정해진 수(예: 5~10명)의 적에 대해서만 A* 알고리즘을 수행하고 결과를 해당 적에게 전달합니다.
       3. 경로 따라가기: 길찾기 결과를 받은 적은 다음 계산 차례가 돌아올 때까지 해당 경로를 따라 이동합니다. 경로가 없는 대부분의 적은 단순히 플레이어
          방향으로 직진합니다.
   * 위기와 해결:
       * 위기(Problem): 모든 적이 매 프레임 A* 알고리즘으로 최적 경로를 계산하려고 시도하자, CPU 연산이 폭증하여 게임이 슬로우 모션처럼 느려진다.
       * 해결(Solution): A* 계산을 소수의 적에게 프레임 단위로 분배(시간 분산)하여, 전체적인 AI의 지능은 유지하면서도 프레임당 계산 부하를 일정하게
         관리합니다.