1. 엔진 프레임워크 구조: Engine → Level → Actor로 이벤트 흐르게 만들기
    

- 엔진 루프에서 프레임을 만들고(타이머로 delta 계산)
    
- 이벤트를 단계적으로 흘림
    
    - BeginPlay: 게임 시작 시 1회
        
    - Tick: 매 프레임 반복(업데이트)
        
    - Draw: 그리기(렌더)
        
- Level은 Actor 포인터들을 모아 관리하고, 이벤트가 오면 Actor 배열을 순회하며 호출
    
- Actor는 가상 함수를 오버라이드해서 “자기 로직”을 구현
    
- 핵심: 상용 엔진(언리얼/유니티)도 큰 뼈대는 결국 이 이벤트 파이프라인을 제공하는 형태
    

2. 실행 중 Actor 추가/삭제가 왜 까다로운가: “순회 중 배열 변경” 문제
    

- Tick 순회 중에 Actor를 추가/삭제하면 배열 크기 변경으로 루프가 깨지거나 예측 불가 동작 발생 가능
    
- 해결 패턴
    
    - 추가 요청 목록(pending spawn) / 삭제 요청 목록(pending destroy)을 따로 모아둠
        
    - 프레임 끝(또는 안전 지점)에서 한꺼번에 반영
        
- 언리얼은 한 단계 더 나아가 GC 흐름
    
    - 삭제 요청 시 즉시 제거 대신 비활성화+플래그 후 “가비지 수거 타이밍(예: 60초 설정 등)”에 회수
        

8. 구현 팁: early-return(가드 패턴)로 예외 상황을 먼저 걸러라
    if(!mainLevel){
	    //log
	   return; 
    }
	    mainLevel->doSomething();

- “레벨이 없으면 return + 로그”처럼 비정상 상황을 먼저 처리하면
    
    - 코드 depth가 줄고
        
    - 예외 처리/로그 지점이 명확해짐
        
- 실무 코드에서 자주 나오는 스타일
    

- 목표
    
    - 기존 콘솔 엔진 프로젝트를 DLL로 빌드
        
    - 게임 프로젝트를 별도로 만들어 DLL + 헤더만 연결해서 사용
        
    - 게임 쪽에서는 엔진 cpp 내부가 안 보이지만, Actor 상속/오버라이드만으로 이벤트가 정상 호출되는 구조를 체험
        
- 빌드 이벤트(Pre/Post build) 활용
    
    - DLL 빌드할 때 헤더/바이너리 자동 복사해서 연동의 귀찮음을 자동화
        
- 기대 효과
    
    - 언리얼의 모듈/매크로/빌드 파이프라인이 “왜 저렇게 복잡한지” 추론 가능한 감각이 생김
        

한 줄 요약

-  Engine→Level→Actor 이벤트 파이프라인을 깔고, 프레임 안정성을 위해 Actor 추가/삭제는 지연 반영 구조로 가져가며, 다음 단계로 DLL 분리까지 가면 상용 엔진의 모듈 구조가 체감된다.