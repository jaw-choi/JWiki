## 업캐스팅/다운캐스팅 정리

### 1) 업캐스팅과 다운캐스팅의 본질

- 캐스팅은 포인터(또는 참조)가 어떤 타입으로 보이게 할지 바꾸는 행위입니다.
    
- 핵심은 객체의 실제 메모리(실체)는 그대로인데, ==접근 가능한 범위와 해석 방식==만 바뀐다는 점입니다.
    

---

### 2) 업캐스팅(자식 → 부모)이 안전한 이유

- 자식 객체는 내부에 부모 부분(베이스 서브오브젝트)을 포함합니다.
    
- 그래서 자식 포인터를 부모 포인터로 바꾸면, 더 큰 객체 중 부모로 보장되는 구간만 접근하게 됩니다.
    
- 결과적으로 컴파일러 관점에서 타입 규칙을 깨지 않고, 런타임에서도 위험한 메모리 침범이 생기지 않습니다.
    
- 업캐스팅의 목적은 보통 다형성: 한 컨테이너/인터페이스로 다양한 자식들을 같은 타입(부모)으로 다루기 위해서입니다.
    

---

### 3) 다운캐스팅(부모 → 자식)이 위험한 이유

- 부모 포인터가 가리키는 실제 객체가 진짜 자식 객체인지 확정할 수 없으면 위험합니다.
    
- 만약 실제 객체가 부모 그 자체인데, 자식으로 캐스팅해 자식 멤버에 접근하면:
    
    - 존재하지 않는 메모리 영역을 자식 멤버라고 착각하고 읽거나 쓰게 됩니다.
        
    - 즉, 허용되지 않은 메모리 접근(논리적 UB)이 발생합니다.
        
- 그래서 다운캐스팅은 실제 동적 타입이 자식임이 보장될 때만 해야 합니다.
- Player* player = new Entity();이것도 가능은하단말? 가능은함.절대안씀.Undefined Behavior의 지름길.  dynamic_cast한다거나. typeID등을 만들어서 보장을 받고 static_cast씀
    

---

### 4) 다운캐스팅을 안전하게 하려면 무엇이 필요한가

- 안전한 다운캐스팅을 위해서는 런타임 타입 정보가 필요합니다.
    
- C++에서는 보통 dynamic_cast가 이 역할을 합니다.
    
    - 변환이 가능하면 자식 포인터를 돌려주고,
        
    - 불가능하면 nullptr(포인터 기준)로 실패를 알려줍니다.
        
- 단, 이런 런타임 검사는 비용이 들어가므로(추가 정보 + 검사), 남용하면 성능과 구조가 나빠질 수 있습니다.
    

---

### 5) 그래서 다형성에는 virtual이 중요하다

- 업캐스팅해서 부모 포인터로 함수를 호출했을 때,
    
    - virtual이 없으면 정적 바인딩: 부모 함수가 호출됩니다.
        
    - virtual이 있으면 동적 바인딩: 실제 객체(자식)의 override가 호출됩니다.
        
- 이게 바로 캐스팅을 덜 하고도 원하는 동작을 끌어낼 수 있는 이유입니다.
    
- 즉, 캐스팅으로 분기하는 대신, 가상 함수로 분기를 맡기는 것이 구조적으로 깔끔합니다.
    

---

### 6) virtual이 추가하는 비용(왜 비용이 생기는지)

- 가상 함수가 있으면 보통 객체에 vptr(가상 테이블 포인터)가 붙고, 클래스에는 vtable이 생깁니다.
    
- 호출 시에는 함수 주소를 테이블에서 찾아 간접 호출합니다.
    
- 그래서:
    
    - 객체 크기 증가 가능
        
    - 호출이 한 단계 간접화
        
- 하지만 대부분의 경우 설계 이득이 비용을 상쇄하고도 남는 편입니다(특히 엔진/게임 오브젝트 구조).
    

---

### 7) 엔진 구조에서 캐스팅이 왜 자주 나오나(현실 예시)

- 엔진은 대개 부모 타입 포인터 배열/리스트로 오브젝트를 일괄 관리합니다.
    
    - 예: vector<Base*>에 모든 GameObject/Actor를 넣고 순회
        
- 그러면 게임 코드는 특정 타입만 골라 특별 처리하려고 다운캐스팅 유혹을 받습니다.
    
- 이때 구조가 나빠지기 쉬운 패턴:
    
    - if (auto p = dynamic_cast<Player*>(obj)) { ... }
        
    - if (auto e = dynamic_cast<Enemy*>(obj)) { ... }
        
    - 이런 식으로 타입 체크 분기가 늘어남
        

---

### 8) 좋은 방향(캐스팅을 줄이는 설계 방향)

- 가능하면 타입 판별(dynamic_cast)로 분기하지 말고, 부모 인터페이스(virtual)로 해결합니다.
    
    - 예: obj->Update(), obj->OnHit(), obj->Render() 같은 식으로
        
- 정말로 타입별 로직이 필요하면:
    
    - 역할 중심 인터페이스(예: IDamageable, IInteractable)
        
    - 컴포넌트 기반 구성(Has-a)
        
    - 방문자(Visitor) 같은 패턴
        
    - 이벤트/메시지 디스패치
        
- 쪽으로 캐스팅을 구조적으로 밀어내는 방식이 더 견고합니다.


==다운캐스팅 vs Virtual==
>	 virtual 함수 없다면 다운캐스팅으로 타입 찾아서 처리
    for (Entity* e : entities)
    {
        if (Player* p = dynamic_cast<Player*>(e))
        {
            p->Heal(10);
        }
    }

>	virtual 함수 있다면 다운캐스팅 없이 바로 호출 ->적절한 함수가 쓰여짐
void Process_ByVirtual(const std::vector<Entity*>& entities)
{
    for (Entity* e : entities)
    {
        e->OnTurn(); // 타입 몰라도 알아서 맞는 override 호출
    }
}




try {
        Derived& d = dynamic_cast<Derived&>(b); // 실패 시 예외
        d.Special();
    } catch (const std::bad_cast&) {
        std::cout << "bad_cast\n";
    }

의미 try catch구문 => 실패하면 std::bad_cast& 라는 오류를 잡아냄(종류 다양)