

## 1. 한 줄 요약

매크로 함수는 전처리기에 의해 코드가 치환되는 방식이고, 인라인 함수는 컴파일러에게 함수 호출 오버헤드를 줄이도록 요청하는 방식이다.

## 2. 왜 필요한가? (Why)

- 성능 향상: 함수 호출 오버헤드를 줄여 실행 속도 개선
- 코드 재사용성: 매크로 또는 인라인 함수를 통해 코드 중복 방지

## 3. 핵심 개념 (What)

- 매크로 함수:
    - 전처리기에 의해 코드가 치환됨
    - 콜 스택을 사용하지 않음
    - 타입 검사를 하지 않음
- 인라인 함수:
    - 컴파일러에게 함수 호출 오버헤드를 줄이도록 요청
    - 실제로 인라인될지는 컴파일러가 결정
    - 타입 검사를 함
- `#define FORCEINLINE __forceinline`: 컴파일러에게 반드시 인라인하도록 지시 (컴파일러에 따라 다름)

## 4. 동작 과정 (How)

- 매크로 함수: 전처리기가 코드를 치환
- 인라인 함수: 컴파일러가 함수 호출 부분을 함수 코드로 대체 (가능한 경우)

## 5. 장단점 / 트레이드오프

- 매크로 함수:
    - 장점:
    - 함수 호출 오버헤드 없음
    - 유연한 코드 생성 가능
    - 단점:
    - 타입 검사 없음
    - 디버깅 어려움
    - 예상치 못한 동작 가능성
- 인라인 함수:
    - 장점:
    - 타입 검사
    - 디버깅 용이
    - 성능 향상
    - 단점:
    - 컴파일러가 인라인을 거부할 수 있음
    - 코드 크기 증가 가능성

## 6. 자주 나오는 질문 / 오해

- Q: 매크로 함수와 인라인 함수 중 어떤 것을 사용하는 것이 좋은가?
- A: 타입 안전성과 디버깅 용이성을 고려하면 인라인 함수가 더 나은 선택이지만, 특수한 경우에는 매크로 함수가 필요할 수 있음

## 7. 프로젝트에서 어디에 쓰일 수 있는가

- 성능 критично한 코드
- 자주 호출되는 작은 함수

## 8. 연관 개념 (Obsidian 링크 형식)

- [CS - 함수 호출](app://obsidian.md/CS%20-%20%ED%95%A8%EC%88%98%20%ED%98%B8%EC%B6%9C)
- [CS - 컴파일 과정](obsidian://open?file=CS%2FCS%20-%20%EC%BB%B4%ED%8C%8C%EC%9D%BC%20%EA%B3%BC%EC%A0%95.md)