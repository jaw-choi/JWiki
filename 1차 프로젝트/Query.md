위에서 quadtree->Insert(actor->As<Enemy>()); 하면 quadtree에 enemy가 배치 되어 있음 이후  특정 객체와 query(vector,객체) 하면 vector에 quadtree내의 오브젝트와 객체가 intersect 하는 것만 모아서 반환됨


quadtree->Query(enemies,  actor->As<Enemy>()); 이런 식으로 했는데 에러남

따라서, quadtree->Query(enemies, player); 이렇게 하니 정상 작동

Query는두번째 인자와 겹치는 부분을 첫번째 인자의 벡터에 넣어주는 역할

 * 쿼드트리 없이: 도서관의 모든 책을 하나하나 뒤져야 합니다 (매우 느림).
   * 쿼드트리(Query 함수)를 사용하면: 먼저 도서관의 전체 배치도(루트 노드)를 보고, 찾는 책이 '소설' 코너에 있다면
     '소설' 코너(자식 노드)로만 이동합니다. 만약 '판타지 소설'이라면 '판타지' 섹션으로만 이동하는 식입니다.
     Query() 함수는 "이 책이 '소설'과 '판타지' 섹션 중 어디엔가 있을 것 같으니, 그와 관련된 모든 책들을
     모아주세요"라고 요청하는 것과 같습니다. 이렇게 하면 전체 도서관의 책이 아니라, 훨씬 작고 관련된 책 목록을
     얻게 되어 효율적으로 찾을 수 있습니다.