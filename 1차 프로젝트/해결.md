   1. `Actor.h` 포함 순서 문제: super, width, height는 Actor 클래스 또는 Actor를 상속받은 클래스의
      멤버들입니다. Quadtree.h를 포함하면서 Actor.h가 예상했던 순서대로 포함되지 않거나, Actor 클래스의
      완전한 정의가 필요하기 전에 참조되어 컴파일러가 Actor의 멤버들을 알지 못하게 된 것입니다.
   2. 순환 참조 또는 불완전한 전방 선언:
       * Quadtree.h는 Actor*를 사용하므로 class Actor; 전방 선언이 필요합니다.
       * Rect.h (또는 다른 헤더)에서 Actor의 멤버 (width, height, GetPosition() 등)를 직접 사용하려 할
         경우, class Actor; 전방 선언만으로는 부족하고 `Actor.h` 전체를 포함해야 합니다. 만약 Rect.h가
         Actor.h를 포함하고 Quadtree.h가 Rect.h를 포함하는데, 어떤 이유로 Actor.h가 제대로 처리되지 않으면
         문제가 생깁니다.
   * 2번 문제가 어떻게 된듯