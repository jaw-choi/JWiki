![[Pasted image 20260210124947.png]]


![[Pasted image 20260210125040.png]]

![[Pasted image 20260210134038.png]]

## 핵심 원인 1

### sourceIndex가 문자열 길이를 초과함 (가장 유력)

이 구조는 보통 이런 흐름입니다.

`int sourceIndex = x - startX;`

그리고 그 다음에

`command.text[sourceIndex];`

문제는 여기입니다.

- 화면에 그릴 영역은 `visibleStart ~ visibleEnd`
    
- 하지만 `command.text` 길이는 고정
    
- **sourceIndex가 text 길이보다 커지는 순간 즉시 터짐**
    

### 재현되는 상황

- 텍스트가 화면 오른쪽/왼쪽으로 잘릴 때
    
- LevelUp UI, Pause 상태에서 Tick(0.f)로 위치 고정될 때
    
- startX가 음수이거나 screen 밖일 때
    

---

## 핵심 원인 2

### index가 frame 버퍼 범위를 벗어남

`int index = (command.position.y * screenSize.x) + x;`

이때 다음 중 하나라도 성립하면 바로 터집니다.

- command.position.y < 0
    
- command.position.y >= screenSize.y
    
- x < 0
    
- x >= screenSize.x
    

**이 값은 charInfoArray 크기를 그대로 벗어납니다.**

---

## 이 코드가 특히 위험한 이유

이 줄은 **방어 코드가 전혀 없습니다.**

`frame->charInfoArray[index].Char.AsciiChar     = command.text[sourceIndex];`

즉

- index 검증 없음
    
- sourceIndex 검증 없음
    

콘솔 게임에서 이건 **언젠가는 반드시 터지는 코드**입니다.
