# AI 활용 코딩테스트 대비 가이드

AI를 도구로 쓰되, 문제 파악과 판단은 사람이 하는 방식을 연습하기 위한 자료입니다.
구성은 다음과 같습니다.

1. 공통 원칙
2. 유형 A: 기존 코드베이스 수정형 (템플릿, 예시 문제, 프롬프트 흐름)
3. 유형 B: 알고리즘 문제형 (템플릿, 예시 문제, 프롬프트 흐름)
4. 과정 기록 템플릿
5. 시험 당일 체크리스트

예시 코드는 Python으로 작성했지만, 접근 방식은 언어와 무관합니다.

---

## 1. 공통 원칙

평가자가 보는 것은 정답 자체보다 아래 네 가지일 가능성이 큽니다.

| 관점 | 평가자가 보고 싶은 것 |
|---|---|
| 문제 이해 | AI 없이도 요구사항과 구조를 스스로 요약할 수 있는가 |
| 분해 | 큰 문제를 검증 가능한 작은 단계로 나눴는가 |
| 지휘 | 맥락, 제약, 산출물, 검증 기준이 담긴 프롬프트를 쓰는가 |
| 검증 | AI 결과를 그대로 믿지 않고 직접 확인하고 고치는가 |

### 기본 흐름

```
[사람] 문제 읽기, 한두 문장 요약, 제약 정리
   ↓
[사람] 단계 분해, 각 단계의 검증 방법 정하기
   ↓
[AI]   단계별로 접근 방식 제안, 구현
   ↓
[사람] 코드 읽기, 테스트 실행, 실패 시 근거를 주고 재요청
   ↓
[사람] 기록 정리 (선택 이유, 수정한 부분)
```

### 좋은 프롬프트의 4요소

1. **맥락**: 문제 요약, 관련 코드, 언어와 환경
2. **제약**: 수정 가능한 범위, 시간 복잡도, 금지 사항
3. **산출물**: 원하는 결과물의 형태 (함수 하나, diff, 접근 비교표 등)
4. **검증 기준**: 통과해야 할 예제와 경계 케이스

### 피해야 할 패턴

- 문제 전문을 붙여 넣고 "풀어줘"만 쓰기
- 요청하지 않은 리팩터링이나 기능이 섞인 코드를 검토 없이 수용하기
- 실패했을 때 같은 프롬프트를 반복하기 (실패 입력, 실제 출력, 기대 출력을 추가해야 함)
- 이해하지 못한 코드를 제출하기

---

## 2. 유형 A: 기존 코드베이스 수정형

주어진 프로젝트를 이해하고 버그를 고치거나 기능을 추가하는 유형입니다.

### 2-1. 접근 템플릿

**Step 0. 직접 파악하기 (AI 사용 전, 5~10분)**

- 진입점은 어디인가
- 모듈 간 호출 관계는 어떤가
- 요구사항에서 건드려야 할 파일은 어디인가
- 이미 있는 테스트는 무엇이고 어떻게 실행하는가

**Step 1. 구조 파악 프롬프트 (선택)**

```
아래는 프로젝트의 파일 구조와 핵심 파일이다.
[파일 목록 및 코드]

내가 파악한 구조는 다음과 같다:
[내 요약]

내 이해가 맞는지 확인해 주고, 놓친 의존 관계가 있으면 알려줘.
코드는 수정하지 마.
```

**Step 2. 버그 수정 프롬프트**

```
[증상] 무엇이 어떻게 잘못되는지
[재현] 입력과 실제 출력, 기대 출력
[범위] 수정 가능한 파일과 함수: OOO
[제약] 다른 코드의 스타일과 구조는 유지하고, 관련 없는 부분은 건드리지 마

먼저 원인 가설을 정리해 주고, 최소 수정안을 diff로 제시해 줘.
```

**Step 3. 기능 추가 프롬프트**

```
[요구사항] 추가할 기능의 명세
[기존 동작] 바뀌면 안 되는 동작
[제약] 기존 함수 시그니처 유지 여부, 추가 가능한 파일
[검증] 통과해야 할 케이스 목록

구현 전에 설계 방향과 가정을 먼저 알려줘. 내가 확인한 뒤 구현해 줘.
```

**Step 4. 테스트 프롬프트**

```
방금 수정한 동작을 검증하는 테스트를 작성해 줘.
- 정상 케이스 / 경계 케이스 / 실패 케이스를 구분해서
- 수정 전 코드에서는 실패하고 수정 후에는 통과해야 해
```

---

### 2-2. 예시 문제: 주문 서비스

#### 상황

간단한 주문 처리 모듈입니다. 아래 세 가지 작업을 하세요.

1. 버그 수정: 주문이 실패했는데 재고가 줄어든 채로 남아 있다는 신고가 들어왔습니다.
2. 기능 추가: 쿠폰 할인을 지원해야 합니다.
3. 테스트: 위 두 가지를 검증하는 테스트를 추가하세요.

#### 코드

`inventory.py`

```python
class OutOfStock(Exception):
    pass


class Inventory:
    def __init__(self, stock):
        self.stock = dict(stock)

    def reserve(self, sku, qty):
        if self.stock.get(sku, 0) < qty:
            raise OutOfStock(sku)
        self.stock[sku] -= qty

    def release(self, sku, qty):
        self.stock[sku] = self.stock.get(sku, 0) + qty
```

`order_service.py`

```python
def place_order(inventory, payment, items, user):
    """items: [(sku, qty, unit_price), ...]"""
    total = 0
    for sku, qty, price in items:
        inventory.reserve(sku, qty)
        total += qty * price
    payment.charge(user, total)  # 실패 시 예외 발생
    return {"user": user, "total": total}
```

#### 요구사항

**버그**: 다음 두 경우에 이미 차감된 재고가 원상 복구되어야 합니다.

- 여러 품목 중 뒤쪽 품목에서 `OutOfStock`이 발생한 경우
- 재고는 충분했지만 `payment.charge`가 예외를 던진 경우

**쿠폰 기능**: `place_order`에 선택 인자 `coupon=None`을 추가합니다.

- 쿠폰은 `{"type": "percent", "value": 10, "max_discount": 5000}` 형태입니다.
- percent 쿠폰은 총액의 value%를 할인하되 `max_discount`를 넘지 않습니다.
- 쿠폰이 없으면 기존 동작과 완전히 같아야 합니다.
- 할인 후 금액이 0 미만이 되면 안 됩니다.

---

### 2-3. 접근 방법과 프롬프트 예시

#### Step 0. 직접 파악한 내용 (사람이 작성)

```
- 진입점: place_order
- 의존: Inventory(reserve/release), payment(charge)
- 버그 위치 추정: reserve는 성공하는데 이후 실패 시 release를 호출하는 곳이 없음
- 이미 있는 release 메서드를 재사용하면 될 것 같음
- 쿠폰은 total 계산 후, charge 호출 전에 적용해야 함
```

이 정도를 스스로 정리해 두면, AI 답변이 맞는지 판단할 기준이 생깁니다.

#### Step 1. 버그 수정 프롬프트

```
Python 주문 처리 코드에서 버그를 고치려고 한다.

[증상]
여러 품목을 주문할 때 뒤쪽 품목이 재고 부족으로 실패하거나,
결제가 실패하면 앞 품목의 재고가 차감된 채로 남는다.

[재현]
재고 {"A": 5, "B": 1}, items=[("A", 2, 1000), ("B", 2, 500)] 주문 시
OutOfStock("B")이 발생하고, 이후 재고가 {"A": 3, "B": 1}이다.
기대 결과는 {"A": 5, "B": 1}이다.

[제약]
- 수정 대상은 order_service.py의 place_order뿐이다.
- Inventory 클래스와 함수 시그니처는 바꾸지 마.
- 이미 있는 Inventory.release를 활용해 줘.
- 예외는 삼키지 말고 원래 예외를 그대로 다시 던져 줘.

원인을 두세 줄로 설명하고, 최소 수정안을 diff로 보여 줘.
```

#### 기대하는 AI 답변 (검토 대상)

```python
def place_order(inventory, payment, items, user):
    total = 0
    reserved = []
    try:
        for sku, qty, price in items:
            inventory.reserve(sku, qty)
            reserved.append((sku, qty))
            total += qty * price
        payment.charge(user, total)
    except Exception:
        for sku, qty in reserved:
            inventory.release(sku, qty)
        raise
    return {"user": user, "total": total}
```

#### 사람이 검토할 포인트

- `reserved`에는 reserve가 성공한 품목만 들어가는가 (실패한 품목을 release하면 재고가 늘어남)
- 예외를 다시 던지는가 (`raise`)
- 결제 성공 후에는 롤백되지 않는가
- `except Exception` 범위가 적절한가 (`BaseException`까지 잡지 않는가)

#### Step 2. 쿠폰 기능 프롬프트

설계를 먼저 받고 구현하는 2단계 방식을 씁니다.

```
방금 수정한 place_order에 쿠폰 기능을 추가하려 한다.

[요구사항]
- 인자 coupon=None 추가 (기본값이므로 기존 호출은 그대로 동작)
- 쿠폰 형태: {"type": "percent", "value": 10, "max_discount": 5000}
- 할인액 = min(total * value / 100, max_discount), 최종 금액은 0 이상
- 쿠폰이 없으면 기존 동작과 동일

[제약]
- 할인 계산은 별도 함수로 분리해도 되지만 파일은 order_service.py 안에서만 처리
- 쿠폰 type이 percent가 아니면 어떻게 처리할지는 네가 정하지 말고 선택지만 알려 줘

구현 전에 다음을 먼저 알려 줘.
1. 할인 적용 위치 (재고 예약 전 / 후, 결제 전)
2. 소수점 처리 방식 선택지와 각각의 장단점
3. 지원하지 않는 쿠폰 타입에 대한 처리 선택지
```

이 프롬프트의 핵심은 **AI가 조용히 결정할 만한 부분(소수점, 알 수 없는 타입)을 미리 표면화**하는 것입니다. 사람이 고른 뒤에 구현을 요청합니다.

```
선택: 소수점은 내림(int), 지원하지 않는 타입은 ValueError.
위 선택대로 구현해 줘. 변경된 부분만 diff로 보여 줘.
```

#### Step 3. 테스트 프롬프트

```
지금까지 수정한 place_order를 검증하는 pytest 테스트를 작성해 줘.

필수 케이스:
1. 정상 주문: 재고 차감, 반환값 확인
2. 뒤쪽 품목 재고 부족: 예외 발생 + 재고 전부 원복
3. 결제 실패: 예외 발생 + 재고 전부 원복
4. 쿠폰 percent: 할인 계산 정확성
5. 쿠폰 max_discount 초과 시 상한 적용
6. coupon=None이면 기존 결과와 동일
7. 지원하지 않는 쿠폰 타입: ValueError

결제 실패는 charge에서 예외를 던지는 가짜 객체(fake)로 만들어 줘.
```

#### 사람이 확인할 것

- 2번과 3번 테스트가 **수정 전 코드에서 실제로 실패하는지** (git stash 등으로 확인하면 가장 확실합니다)
- 테스트가 구현 내용을 그대로 베낀 것은 아닌지 (기대값이 직접 계산한 값인지)

#### 자주 나오는 실패와 재프롬프트 예시

AI가 쿠폰 할인을 `reserve` 루프 안에서 매 품목마다 적용해 버렸다면:

```
테스트 4가 실패한다.
입력: items=[("A", 2, 1000), ("B", 1, 3000)], coupon 10% (max 5000)
실제 출력: total = 4500
기대 출력: total = 4500

... (숫자는 상황에 맞게)
```

실패 로그를 그대로 주고 "어느 줄에서 할인이 중복 적용되는지 짚어 줘"처럼 원인 분석을 먼저 요청하면 됩니다.

---

## 3. 유형 B: 알고리즘 문제형

새 문제를 읽고 접근 방식을 정한 뒤 구현하는 유형입니다. 이 유형에서는 **접근 방식 비교와 복잡도 판단**이 특히 중요합니다.

### 3-1. 접근 템플릿

**Step 0. 직접 파악하기 (AI 사용 전)**

```
- 입력 크기 범위: N ≤ ?, Q ≤ ?
- 시간 제한: ?
- 무식한 풀이의 복잡도: O(?)  → 통과 가능한가
- 의심되는 자료구조나 기법: ?
```

**Step 1. 접근 비교 프롬프트**

```
[문제 요약] 두세 문장
[입력 제약] N ≤ ..., 시간 제한 ...
[내가 생각한 무식한 풀이] ... O(...)

구현하지 말고 접근 방식을 2가지 비교해 줘.
각각 시간/공간 복잡도, 핵심 아이디어, 구현 시 실수하기 쉬운 부분을 알려 줘.
```

**Step 2. 구현 프롬프트**

```
접근 B로 구현해 줘.
- 언어: Python 3
- 입출력 형식: [형식]
- 경계 조건: [빈 입력, 동일 값 등]
- 함수로 분리해서 테스트하기 쉽게 작성

코드와 함께 핵심 아이디어를 세 줄 이내로 설명해 줘.
```

**Step 3. 검증 프롬프트**

```
이 풀이의 반례를 찾아 줘.
- 경계 케이스 5개
- 무식한 풀이와 결과를 비교하는 랜덤 테스트 코드도 작성해 줘
```

---

### 3-2. 예시 문제: 서버 동시 접속자 분석

#### 문제

서버의 세션 기록이 `N`개 주어집니다. 각 기록은 `(start, end)`이며 시각 `start` 이상 `end` 미만 동안 접속했음을 뜻합니다(반열림 구간 `[start, end)`).

두 가지를 구하세요.

1. 최대 동시 접속자 수
2. `M`개의 시각 `t`가 주어질 때, 각 시각의 동시 접속자 수

#### 제약

- 1 ≤ N ≤ 200,000
- 1 ≤ M ≤ 200,000
- 0 ≤ start < end ≤ 10^9
- 시간 제한 2초

#### 입력 예시

```
sessions = [(1, 5), (2, 6), (5, 8), (7, 9)]
queries  = [0, 1, 2, 5, 6, 8, 9]
```

#### 출력 예시

```
최대 동시 접속자: 2
질의 결과: [0, 1, 2, 2, 1, 1, 0]
```

`t=5`일 때 `(1,5)`는 끝났고(반열림), `(2,6)`과 `(5,8)`이 접속 중이므로 2입니다.

---

### 3-3. 접근 방법과 프롬프트 예시

#### Step 0. 직접 파악한 내용 (사람이 작성)

```
- N, M 모두 최대 2×10^5
- 질의마다 모든 세션을 훑으면 O(N×M) = 4×10^10 → 시간 초과
- 정렬 + 이분 탐색, 또는 스위핑(sweep line)이 후보
- 반열림 구간이라 경계(start == t, end == t) 처리가 핵심 함정
```

#### Step 1. 접근 비교 프롬프트

```
서버 세션 [start, end) 구간 N개가 있고,
(1) 최대 동시 접속자 수와 (2) M개 시각 각각의 동시 접속자 수를 구하는 문제다.
N, M ≤ 2×10^5, 시간 제한 2초, 좌표는 최대 10^9.

내 판단으로 질의마다 전체를 훑는 O(N×M)은 불가능하다.
구현하지 말고 대안 2가지를 비교해 줘.
각각 시간/공간 복잡도와 반열림 구간에서 실수하기 쉬운 지점을 알려 줘.
```

#### 기대하는 AI 답변 요약

| 접근 | 아이디어 | 복잡도 |
|---|---|---|
| 1. 시작/종료 시각 각각 정렬 + 이분 탐색 | 시각 t의 접속자 수 = (start ≤ t인 개수) - (end ≤ t인 개수) | O((N+M) log N) |
| 2. 이벤트 스위핑 | 시작 +1, 종료 -1 이벤트를 정렬해 순회, 동시 종료/시작 시 종료를 먼저 처리 | O((N+M) log(N+M)) |

#### 사람이 판단하는 부분

```
최대 동시 접속자와 질의 둘 다 필요하니 정렬된 start/end 배열 하나로
둘 다 풀 수 있는 접근 1을 선택. 최대값은 종료 이벤트를 먼저 처리하는
스위핑으로 따로 계산해도 되지만, 접근 1로 start 시각들에서만 검사해도
충분하다는 점을 AI에게 확인시키고 싶음.
```

#### Step 2. 구현 프롬프트

```
접근 1(정렬 + bisect)로 구현해 줘.

[함수 시그니처]
def analyze(sessions, queries) -> (int, list[int])

[요구사항]
- 최대 동시 접속자 수는 start 시각들에서만 검사해도 되는지 근거를 한 줄로 알려 주고 그대로 구현
- 구간은 [start, end) 반열림
- bisect 모듈 사용
- 입력을 수정하지 말 것

코드 뒤에 핵심 아이디어를 세 줄 이내로 설명해 줘.
```

#### 기대하는 AI 답변 (검토 대상)

```python
from bisect import bisect_right


def analyze(sessions, queries):
    starts = sorted(s for s, _ in sessions)
    ends = sorted(e for _, e in sessions)

    def count_at(t):
        # start <= t 인 세션 수 - end <= t 인 세션 수
        return bisect_right(starts, t) - bisect_right(ends, t)

    # 동시 접속자 수의 최댓값은 어떤 세션의 시작 시각에서 달성된다
    max_concurrent = max(count_at(s) for s in starts)
    return max_concurrent, [count_at(t) for t in queries]
```

#### 사람이 검토할 포인트

- `bisect_right(ends, t)`가 맞는지 확인: `end == t`인 세션은 이미 끝난 것이므로 빼야 합니다. `bisect_right`는 `end <= t`인 개수를 반환하므로 맞습니다. `bisect_left`를 쓰면 틀립니다.
- 최대값이 start 시각에서만 검사해도 되는 이유: 접속자 수가 증가하는 순간은 항상 어떤 세션의 시작 시각입니다.
- 복잡도: 정렬 O(N log N) + 질의 O((N+M) log N)으로 제한 내입니다.
- 예제 손검산: `t=5`에서 `bisect_right(starts=[1,2,5,7], 5)=3`, `bisect_right(ends=[5,6,8,9], 5)=1`, 따라서 2

#### Step 3. 검증 프롬프트

```
이 풀이의 반례와 검증 코드를 만들어 줘.

1. 경계 케이스 5개: 세션 1개, 모든 세션이 같은 구간,
   start == end인 인접 세션(예: [1,3)과 [3,5)), 질의가 구간 밖,
   최대 좌표(10^9)
2. O(N×M) 무식한 풀이와 결과를 비교하는 랜덤 테스트 코드
   (작은 범위의 랜덤 입력 1000회)
```

#### 실패 시 재프롬프트 예시

AI가 `bisect_left`로 구현했다면 예제에서 `t=5`가 3으로 나올 것입니다.

```
예제에서 t=5일 때 3이 나온다 (기대값 2).
sessions=[(1,5),(2,6),(5,8),(7,9)], t=5
bisect_right(starts,5)=3, bisect_left(ends,5)=0
원인은 종료 시각 처리에 있는 것 같은데, 반열림 구간 기준으로
어느 함수를 써야 하는지 근거와 함께 알려 줘.
```

---

## 4. 과정 기록 템플릿

제출 형식에 기록란이 있거나, 없더라도 스스로 정리해 두면 좋습니다.

```markdown
## 문제 이해
- 요약:
- 제약:
- 무식한 풀이와 한계:

## 접근 선택
- 후보 1:
- 후보 2:
- 선택과 이유:

## AI 활용 기록
| 단계 | 프롬프트 요지 | AI 결과 | 내가 검토/수정한 점 |
|---|---|---|---|
| 1 | | | |
| 2 | | | |

## 검증
- 실행한 테스트:
- 발견한 문제와 해결:

## 회고
- 잘한 점:
- 다음에 다르게 할 점:
```

---

## 5. 시험 당일 체크리스트

**시작 전**

- [ ] 문제 전문과 제약을 끝까지 읽었다
- [ ] 한두 문장으로 요약할 수 있다
- [ ] 무식한 풀이의 복잡도를 계산했다
- [ ] AI에게 맡길 것과 내가 판단할 것을 나눴다

**AI 사용 중**

- [ ] 한 번에 한 단계씩 요청한다
- [ ] 프롬프트에 맥락, 제약, 산출물, 검증 기준이 있다
- [ ] 구현 전에 접근이나 설계를 먼저 확인한다
- [ ] AI가 조용히 결정한 부분(가정)이 없는지 확인한다

**제출 전**

- [ ] 예제와 경계 케이스를 직접 실행했다
- [ ] 모든 코드를 내가 설명할 수 있다
- [ ] 요청하지 않은 변경이 섞이지 않았다 (diff 확인)
- [ ] 과정 기록을 정리했다

---

## 6. 연습 방법 제안

1. 유형 A 예시를 AI 없이 먼저 풀어 보고, 그다음 AI를 쓰면서 시간과 품질을 비교합니다.
2. 같은 문제를 "통째로 붙여 넣기"와 "단계별 프롬프트"로 각각 시도해서 결과 차이를 확인합니다.
3. 프롬프트를 일부러 나쁘게 써서 AI가 어떻게 엉뚱한 결과를 내는지 경험해 봅니다. 그래야 시험에서 위험 신호를 알아챌 수 있습니다.
4. 타이머를 걸고(유형 A 60분, 유형 B 40분) 기록 템플릿까지 채우는 연습을 합니다.


  
내용 요약  
[**일하는 문화**](https://blog.nexon.com/working-culture)[**게임과 기술**](https://blog.nexon.com/game-and-technology)[**도전의 가치**](https://blog.nexon.com/game-worth-the-challenge)

[company logo](https://blog.nexon.com/)

**일하는 문화**

**AI 시대, 개발∙기획 시니어 구성원이 말하는 일의 본질**

2026.07.16

svg

넥슨코리아 AI본부 소속 오윤호 님은 10년 넘게 기술 면접을 맡아왔습니다. 수백 명의 포트폴리오를 검토했고, 수천 개의 질문을 던졌죠. 각기 다른 장점을 가진 수많은 인재들 중 누가 해당 직무에 가장 잘 어울릴지 판단하는 것은 늘 어려운 일이지만, 그 과정에서 한 가지는 점점 더 분명해졌습니다. 지원자가 어떤 답을 내놓았는지가 아니라, 어떤 과정을 거쳐 그 답에 도달했는지가 더 중요하다는 사실이었습니다.

오랫동안 게임 기획을 맡아온 메이플M글로벌실 박용규 님 역시 비슷한 이야기를 했습니다. 좋은 기획서를 평가할 때 완성도보다 그 안에 담긴 고민과 의도를 먼저 본다고요.

누구나 쉽게 다양한 정보를 찾아볼 수 있고, AI가 코드를 작성해주는 시대. 그렇다면 좋은 개발자와 기획자를 만드는 본질적인 경쟁력은 무엇일까요? 넥슨에서 오랜 기간 사람을 뽑고, 게임을 만들어온 두 시니어 구성원에게 그 답을 물었습니다.

**포트폴리오보다 중요한 고민과 질문의 경험**

“왜 그렇게 생각했나요?”

“왜 그 방법을 선택했나요?”

오윤호 님이 면접에서 가장 자주 던지는 질문입니다. 지원자가 어떤 기술을 사용했는지보다, 왜 그런 선택을 했는지에 주목하는 것이죠.

“스스로 치열하게 고민해서 몸에 익힌 지식보다는, 정답을 암기해서 온 경우가 많기 때문이죠. 예를 들어 의자를 만들어봤다는 지원자에게 어떻게 만들었냐고 물었을 때, 단순히 옆에 있는 의자를 보고 똑같이 만들었다고 답하는 사람과 ‘왜 의자 다리는 2개나 5개가 아니라 4개여야만 할까?’를 고민해 본 사람은 근본적인 차이가 있습니다.”

많은 지원자가 자신이 무엇을 만들었는지는 설명할 수 있습니다. 하지만 왜 그 방법을 선택했는지, 다른 대안은 없었는지, 그리고 그 선택에 어떤 장단점이 있었는지를 묻는 순간 답변이 막히는 경우가 적지 않다고 합니다.

중요한 것은 정답을 맞히는 일이 아닙니다. **자신이 알고 있는 지식을 바탕으로 어떤 가설을 세우고 어디까지 추론할 수 있는가입니다**. 검색 몇 번이나 AI 프롬프트 입력만으로 그럴듯한 정답 코드를 얻을 수 있는 시대이기에, 이 사고 과정의 가치는 이전보다 훨씬 귀해졌습니다.

이러한 시선은 기획 직군에서도 크게 다르지 않습니다. 박용규 님 역시 포트폴리오를 볼 때 결과물보다 그 안에 담긴 생각의 흔적을 먼저 살펴본다고 말합니다.

“같은 기획안이라도 왜 이런 시스템을 넣었는지, 플레이어에게 어떤 경험을 주고 싶었는지가 드러나는 기획서가 있어요. 반대로 문서는 잘 정리되어 있는데 정작 기획자의 생각은 보이지 않는 경우도 있죠. 저는 후자보다 전자를 더 높게 평가합니다.”

그는 기획자의 일은 수많은 선택지 가운데 하나를 결정하는 과정이라고 설명합니다. 그렇기 때문에 결과보다 중요한 것은 그 선택에 이르게 된 고민의 깊이입니다. **왜 다른 방향이 아닌 지금의 방향을 택했는지가 결국 기획자의 역량을 보여준다는 것**입니다.

결국 직군을 막론하고 시니어들이 확인하고 싶은 것은 하나입니다. 무엇을 만들었는가보다 왜 그렇게 만들었는가. 결과물 자체보다 그 결과물에 이르기까지의 사고 과정입니다.

[image](https://dszw1qtcnsa5e.cloudfront.net/community/20260713/67aef7e2-baa9-452b-8c37-13b0e47839ce/%EA%B7%B8%EB%A6%BC2.jpg)△ 넥슨코리아 AI본부 메이플AX실 오윤호 님 인터뷰 모습

**AI 시대에도 기본기가 유효한 이유**

최근 취업 시장에서 만나는 주니어들의 포트폴리오는 과거와 비교하기 어려울 정도로 상향 평준화됐습니다. 다양한 엔진과 최신 기술 스택을 활용한 프로젝트 경험도 흔해졌죠. 하지만 역설적으로 기술에 대한 깊은 이해를 할 수 있는 기회는 오히려 줄어든 면도 있다고 합니다.

그 배경에는 AI뿐 아니라 엔진과 프레임워크의 발전도 있습니다. 결과물을 만드는 일은 쉬워졌지만, 기초 단계의 문제를 직접 부딪치고 시행착오를 겪으며 해결해보는 기회는 줄어들고 있다는 것입니다.

“AI가 쉽게 답을 제시하면 사람은 그것을 이해했다고 착각하기 쉽습니다. 하지만 현업에서는 기술의 한계를 파악하고 현실적인 타협점을 찾아야 하는 순간이 반드시 옵니다. 알고리즘이나 운영체제 같은 기술의 뼈대를 이해하고 문제의 원인을 추론하는 능력은 여전히 중요한 가치입니다.”

도구는 점점 편리해지지만 문제를 해결하려면 결국 내부 원리를 파악해야 하는 상황, 오윤호 님은 이를 ‘추상화의 역설’이라고 설명합니다.

한편 박용규 님은 기획자의 기본기를 생각을 구조화하는 힘이라고 말합니다.

“신입과 경력의 차이는 정보를 얼마나 많이 아느냐보다 무엇이 중요한 정보인지 구분하는 데서 드러나는 것 같아요. 경력이 쌓일수록 자연스럽게 우선순위를 정하고, 여러 의견 속에서 핵심을 추려내는 능력이 생기죠.”

그는 **좋은 기획자를 단순히 많은 정보를 가진 사람이 아니라, 정보와 정보 사이의 연결고리를 발견하는 사람**이라고 설명합니다.

[image](https://dszw1qtcnsa5e.cloudfront.net/community/20260713/10290cf9-6d07-4936-9209-572d890d7db8/%EA%B7%B8%EB%A6%BC3.jpg)△ 넥슨코리아 메이플M글로벌실 박용규 님 인터뷰 현장

"자신의 생각은 계속 바뀔 수 있고, 다른 사람에게서 들은 이야기가 나중에 중요한 단서가 될 수도 있어요. 그래서 단순히 메모하는 것보다 왜 그런 이야기가 나왔는지, 어떤 맥락에서 나온 생각인지를 함께 정리하는 습관을 추천해드려요.”

최신 트렌드라서 시스템을 추가하는 것과 우리 게임의 핵심 재미를 기준으로 그 시스템이 왜 필요한지 고민하는 것은 전혀 다른 이야기입니다. 질문이 빠진 기획은 겉보기에는 그럴듯할지 몰라도 플레이어에게는 설득력을 갖기 어렵습니다.

결국 좋은 개발자와 기획자는 표면적인 현상보다 그 이면의 이유를 들여다보는 사람입니다. 기술을 이해하는 힘, 사람을 이해하는 힘, 그리고 흩어진 정보를 연결해 의미를 만들어내는 힘. 두 시니어가 말하는 기본기는 AI가 발전할수록 오히려 더 중요해지는 역량이었습니다.

**AI 시대에 더 중요해진 것들**

그렇다면 AI가 실무의 많은 부분을 담당하게 될 앞으로의 시대에 일하는 사람들은 어떤 태도를 가져야 할까요?

오윤호 님은 하나의 기준을 제시합니다.

“AI가 만들어낸 결과물을 스스로 이해하고 검증할 수 있는가?”

그는 최근 호주 여행 중 겪은 경험을 예로 들었습니다. 사진 속 코알라를 찾기 위해 AI 비전 모델을 사용했는데, 실제로는 코알라가 없었음에도 AI는 코알라가 있다고 답했습니다. 확인해 보니 AI가 나뭇가지 사이에 코알라처럼 보이는 형상을 새롭게 그려 넣고 그것을 코알라라고 인식하고 있었던 것입니다.

“질문이 길어지면 앞서 제시한 조건을 놓치거나 존재하지 않는 답을 그럴듯하게 만들어내는 경우가 있습니다. AI 모델에서는 이런 현상이 생각보다 자주 발생해요.”

문제는 AI의 실수가 아니라 그것을 받아들이는 사람의 태도입니다. 결과를 검증하지 않은 채 그대로 실무에 적용한다면 예상치 못한 오류와 리스크가 발생할 수 있습니다.

오윤호 님은 **“좋은 질문은 깊은 이해에서 나오고, 좋은 결과물은 좋은 질문에서 시작됩니다”**라고 말합니다. AI를 경쟁자가 아닌 도구로 바라봐야 한다는 그의 생각도 같은 맥락입니다. AI를 효과적으로 활용하기 위해서도 결국 프로그래밍 언어와 자료구조, 시스템 구조에 대한 이해가 필요하기 때문입니다.

[image](https://dszw1qtcnsa5e.cloudfront.net/community/20260714/8114a2bd-5fe1-498d-8006-44950d357967/%EC%9D%BC%EC%9D%98%EB%B3%B8%EC%A7%88%EB%B3%B8%EB%AC%B8%EC%9D%B4%EB%AF%B8%EC%A7%80.jpg)△ AI가 사실과 다른 정보를 그럴듯하게 생성한 ‘코알라 에피소드’ 대화 내용을 재구성한 이미지

기획자의 고민도 크게 다르지 않습니다.

“기획에는 원래부터 정답이 없어요. AI는 다양한 선택지와 근거를 제시할 수 있지만, 그중 무엇이 우리 게임에 맞는지, 어떤 경험을 플레이어에게 주고 싶은지는 결국 사람이 판단해야 합니다.”

박용규 님은 AI가 발전할수록 기획자의 역할이 더 선명해지고 있다고 말합니다. 과거에는 답을 찾는 것이 어려웠다면 지금은 답이 너무 많아서 문제인 시대가 되었기 때문입니다.

“결국 중요한 건 어떤 질문을 던질 것인가입니다. 같은 AI를 사용해도 질문이 다르면 결과물도 완전히 달라지거든요.”

또한 그는 **AI 시대일수록 차별화된 관점과 문제 정의 능력이 더욱 중요해질 것**이라고 말합니다.

“AI가 제안하는 답은 대부분 이미 존재하는 정보와 사례를 기반으로 만들어집니다. 하지만 새로운 재미를 만드는 일은 결국 기존에 없던 질문을 던지는 것에서 시작된다고 생각해요.”

[image](https://dszw1qtcnsa5e.cloudfront.net/community/20260713/92952c7b-e95f-4fc5-bd2d-3af90bad8e46/%EA%B7%B8%EB%A6%BC1.jpg)△ NDC26에서 진행된 오윤호 님의 세션 ‘기술 면접은 무엇을 평가하는가’ 발표자료 화면

오윤호 님 역시 AI 활용 자체를 부정하지는 않았습니다. 다만 문제를 이해하고 해결하는 과정까지 AI에 맡기는 순간, 스스로 질문하고 판단하는 능력이 약해질 수 있다고 지적했습니다.

“AI 덕분에 구현은 쉬워졌습니다. 하지만 그것이 즉시 내 실력이 되는 것은 아니에요. AI가 만들어준 결과물을 보고 내가 할 줄 안다고 착각하는 순간 성장은 멈출 수 있습니다.”

두 시니어 구성원이 강조한 것은 결국 기술 활용법보다 먼저 갖춰야 할 기본기였습니다. 문제를 이해하는 힘부터 맥락을 읽는 힘, 그리고 결과물을 스스로 검증하고 판단하는 힘까지. AI가 발전할수록 오히려 더 중요해지는 역량들입니다.

AI 시대에도 변하지 않는 경쟁력은 어쩌면 자신만의 질문을 품고 끝까지 탐구해 나가는 힘인지도 모릅니다. 이번 인터뷰에서 만난 두 시니어가 말한 일의 본질은 결국 그 지점에 닿아 있었습니다.

# 연관 콘텐츠

**Family Site**

s

핵심은 **“AI 시대일수록 결과물보다 사고 과정과 기본기가 더 중요하다”**입니다.

### 핵심 요약

- 넥슨의 시니어 개발자·기획자는 지원자를 볼 때 **무엇을 만들었는지보다 왜 그렇게 만들었는지**를 중요하게 봅니다.
    
- 면접에서도  
    **“왜 그렇게 생각했나요?”**,  
    **“왜 그 방법을 선택했나요?”**  
    같은 질문을 통해 지원자의 사고 과정을 확인합니다.
    
- 중요한 것은 정답 암기가 아니라, 자신이 아는 지식으로 **가설을 세우고 추론하며 대안을 비교할 수 있는 능력**입니다.
    
- 개발자는 AI·엔진·프레임워크가 편리해져도 **알고리즘, 운영체제, 자료구조, 시스템 구조 같은 기본기**를 이해해야 문제의 원인과 기술의 한계를 판단할 수 있습니다.
    
- 기획자는 많은 정보를 아는 것보다 **중요한 정보를 골라내고, 정보 사이의 연결고리를 발견하며, 생각을 구조화하는 능력**이 중요합니다.
    
- AI는 여러 답과 선택지를 빠르게 제공하지만, **그 결과가 맞는지 검증하고 실제 상황에 적합한지 판단하는 것은 사람의 역할**입니다.
    
- AI가 만든 코드를 그대로 사용하는 것과 **그 코드의 원리·한계·장단점을 이해하는 것**은 다릅니다.
    
- AI 시대에는 답을 찾는 능력보다 **좋은 질문을 만드는 능력, 문제를 정의하는 능력, 자신만의 관점**이 더욱 중요해집니다.
    
- AI를 사용하지 않는 것이 중요한 것이 아니라, **AI를 도구로 사용하면서도 사고와 판단까지 넘기지 않는 것**이 중요합니다.
    

### 한 문장으로 정리

> **넥슨이 말하는 AI 시대의 경쟁력은 ‘AI로 결과물을 빠르게 만드는 능력’이 아니라, 문제를 이해하고 왜 그런 선택을 했는지 설명하며 AI 결과까지 스스로 검증할 수 있는 능력입니다.**

특히 게임 프로그래머 면접 기준으로 바꾸면 **프로젝트를 설명할 때 `문제 → 원인 분석 → 여러 해결 방법 → 내가 이 방법을 선택한 이유 → 결과 → 한계/개선점` 순서로 말하는 것이 중요하다**고 볼 수 있습니다.