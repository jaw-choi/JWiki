1. 그냥 public 변수만 쓸 때의 문제
    

`public class Player {     public int Hp; }`

사용하는 쪽

`player.Hp = -999;   // 말도 안 되는 값도 그냥 들어감`


규칙을 강제할 지점이 없습니다. 누가 언제 Hp를 망가뜨렸는지 추적도 어렵습니다.

2. 프로퍼티로 규칙 강제(클램프 + 변경 감지)
    
```c++
public class Player
{
    private int hp;
    public int MaxHp { get; } = 100;

    public int Hp
    {
        get => hp;
        set
        {
            int clamped = Math.Clamp(value, 0, MaxHp);
            if (hp == clamped) return; // 같은 값이면 아무것도 안 함
            hp = clamped;
            // 여기에서 UI 갱신, 로그, 이벤트 호출 등을 붙일 수 있음
        }
    }
}
```
player.Hp = -999;   // 실제로는 0으로 들어감
player.Hp = 9999;   // 실제로는 100으로 들어감