[상속]
상속을 쓰면 ->계층이 생김->복잡도 늘어남 ->*목적* 코드의 중복의 방지
상속 -> 계층
비상속 -> delegate,함수포인터
물려줄것 -> 데이터(변수),함수

[클래스 상속]
- 자손으로 내려갈수록 필요한 저장공간의 크기가 커진다(자식은 부모의 정보를 다쓸수있기 때문(private제외))
- 자손은 언제나 부모로 치환가능하다(형변환 가능하다)
- 자손을 부모로 형변환 하는 ==업캐스팅==은 안전, 항상 가능
- Parent ⊃ Child
- Casting -> 낚싯대 던지는거 -> 될수도 안될수도 
- (업캐스팅 100%)
- (다운캐스팅 - 될수도안될수도) 문제는 항상 다운캐스팅 - 다형성 ( Is - a 관계)
- 
- 

SOLID - D(Dependenct inversion principle - .의존성 역전 원칙)
Dependency Injection ->상속안하고 Has a 해서 특정 함수에 다른 객체의 함수를 쓴ㄴ것
외부로부터 내객체가 주소가 주입이되는(거꾸로) 방식
매개변수로 다른 객체를 받아와서 그객체의 함수를 쓰는 방식 등

RTTI 및 Reflection(property system)
![[Pasted image 20260126112611.png]]
어떤 타입을 다른타입으로 저장했다(자식을 부모로) -> 실제 공간의 주소 내가 형변환할 타입이였는지 검사해야함 ->RTTI ()
c#의 'as' -> 'RTTI'
다운캐스팅할때 dynamic_cast만 써야함
대표적으로 RTTI를 쓰는 기능이 2개입니다.

- dynamic_cast
    
- typeid (typeid(expr), typeid(T))