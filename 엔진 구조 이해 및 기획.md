
   * 핵심 목표: 기존 엔진의 아키텍처를 파악하고, 뱀파이어 서바이버즈 스타일 게임 구현에 필요한 기능들을 큰 그림에서 구상한다.
   * 달성 목표:
       * 제공된 파일 구조를 바탕으로 엔진의 핵심 구성 요소(Engine, Level, Actor, Renderer, ScreenBuffer) 간의 관계를 파악.
       * 주요 게임 기능(대형 맵, 오브젝트 풀링, 쿼드트리, 최적화된 AI)에 대한 개념적 이해.
       * 전반적인 개발 계획 수립.

한 일 : SetConsoleWindowInfo하고 SetConsoleScreenBufferSize할때 screen과 buffer의 크기를 다르게 해서 (buffer는 크게하고 screen은 작게 한 후 카메라를 붙여서 맵을 탐방할 목적) 할려했으나 버퍼크기와 스크린 크기가 다를 시 오류가 생김 ㅠㅠ.. ...인 줄 알았으나 임의로 		if (!SetConsoleScreenBufferSize(buffer, mapSize))
		{
			std::cerr << "Failed to set console buffer size.\n";
			__debugbreak();
		}
		라는 가드를 만들었기 때문에 중단이 되었었고 이를 없애면 가능은 하긴함
		->이후 player,enemy보이는 기준을 screen 크기가 아니라 buffer 크기로?
		아님. 그냥 기존대로 screen 과 buffer크기는 똑같이하고 이것을 크게 하되 console사이즈를 screenSize보다 작게 고정한 후 camera로 따라다니게 하면 목적 달성할듯. 휴 
	폰트사이즈 줄이면 그래픽 좋게보일수있음
	창 최대화 시킬 순있음