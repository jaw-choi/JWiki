람다는 객체다
람다함수(무명함수)
[] // lambda capture
() // 파라미터 정의
{} // 함수 본문
(); // 함수 호출
-> 타입 // 반환형 지정(옵션).



auto를 사용하면 람다를 변수에 대입할 수 있다.
```c++

#include <iostream>

int main()
{
	auto function = [](){ std::cout << "Hello World!\n"; };
	function();

	std::cin.get();
}

### 캡처 (일종의 클로저)
```
- 람다를 사용할 때 람다 외부에 정의돼 있는 변수를 람다 내부에서 사용하고 싶을 때는 캡처(Capture)하면 된다.
- 캡처는 참조나 복사로 전달이 가능하다.
- 참조를 사용할 때는 ‘&’ 기호를 사용하고 복사로 전달할 때는 ‘변수 이름’을 사용한다.
- 람다의 ‘[](파라미터){식}’에서 맨 앞의 [] 사이에 캡처할 변수를 작성한다.
- [&변수]하면 참조로 변수 가져옴
- [&변수1,&변수2]하면 참조로 변수1,변수2 가져옴
- [&]하면 참조가능한 변수 다가져옴
