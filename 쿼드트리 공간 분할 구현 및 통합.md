
   * 핵심 목표: Day 3에서 구현한 AABB 충돌 감지 로직의 성능을 향상시키기 위해 쿼드트리(Quadtree) 공간 분할 자료구조를 구현하고 게임에 통합한다.
   * 구현 내용:
       1. `Quadtree` 클래스 구현:
           * Engine/Util 또는 Engine/Common에 Quadtree.h/Quadtree.cpp를 추가하고 Quadtree 클래스를 구현합니다.
           * 클래스는 AABB Bounds, std::vector<Actor*> (현재 노드에 속한 객체들), Quadtree* Children[4] (자식 노드들)를 가집니다.
           * Insert(Actor* actor), Query(const AABB& area), Clear() 메서드를 구현합니다. Insert 시 일정 객체 수 초과 시 4분할(subdivide) 로직을 포함합니다.
       2. `GameLevel`에 쿼드트리 통합:
           * GameLevel에 Quadtree 인스턴스를 생성하고, 맵의 전체 크기를 루트 노드의 경계로 설정합니다.
           * GameLevel의 Update 루프 시작 부분에서 매 프레임 quadtree.Clear()를 호출합니다.
           * 모든 활성화된 Actor들을 quadtree.Insert(actor)를 통해 쿼드트리에 삽입합니다.
       3. 충돌 감지 최적화:
           * Day 3의 충돌 감지 루프를 수정합니다. PlayerBullet가 Enemy와 충돌하는지 검사할 때, 모든 Enemy를 순회하는 대신:
               * 총알의 AABB를 기준으로 quadtree.Query(bullet.GetBoundingBox())를 호출하여 충돌 가능성이 있는 `Enemy` 목록을 얻어냅니다.
               * 이 목록에 대해서만 AABB Intersects 검사를 수행합니다.
   * 테스트:
       * 많은 수의 적과 총알이 있을 때도 게임이 원활하게 작동하는지 확인.
       * 디버그 모드에서 쿼드트리가 객체들을 올바르게 분할하고, Query 결과가 정확한지 (너무 많지도 적지도 않은지) 확인. (콘솔에 쿼드트리 구조를 단순화하여
         출력하는 것도 한 방법)
   * 위기와 해결:
       * 위기(Problem): 쿼드트리 구현이 복잡하여 객체 삽입/질의 시 논리 오류가 발생하거나, 쿼드트리 자체가 오버헤드를 유발하여 성능 개선 효과가 미미하다.
       * 해결(Solution): 쿼드트리 로직을 작은 단위로 쪼개어 구현하고 각 단계마다 철저히 테스트합니다. 특히 Insert 시 노드 분할(subdivide) 조건과 Query 시
         자식 노드 순회 로직의 정확성에 집중합니다. 초기에는 간단한 객체 몇 개로 테스트하여 기본 동작을 확인합니다.