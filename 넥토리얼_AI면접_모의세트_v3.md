# 2026 넥토리얼 Game Programmer AI면접 모의 세트 (90분 대비) v3

포트폴리오(jaw-choi.github.io) 원문 기준으로 트러블슈팅 디테일을 정정하고, LTF 외 다른 프로젝트(Project GGD, 콘솔 뱀서라이크, 눈덩이를 굴려라, Shading Project, BIT SAVER)도 나올 수 있어 추가했습니다.
질문 → 꼬리질문 → 모범답안 → 압박질문 순서로 소리 내어 연습하세요. 모범답안은 뼈대만 외우고 나머지는 그 자리에서 풀어내는 연습이 중요합니다.

---

## 0. 먼저 맞추고 갈 것 — 자소서 서사와 포트폴리오 원문의 온도 차

자소서는 이야기를 압축해서 쓴 것이고, 포트폴리오는 실제 기술 항목을 나열한 것입니다. 꼬리질문은 포트폴리오 쪽 디테일을 파고들 가능성이 높으므로 아래 두 가지를 미리 맞춰두세요.

### (1) LTF 이동/점유 문제 — 정확한 3단 구조
1. **구조 변경**: 유닛별 개별 판단 → CombatManager가 모든 이동 요청을 수집해 중앙에서 처리
2. **점유 충돌 해결**: 요청 순서와 이동 속도를 기준으로 **승인 / 대기 / 재경로 계산**을 판정 (bounce-back 아님, 이 표현은 쓰지 말 것)
3. **조회 최적화**: 좌표 기반 타일 인덱스 Map + 거리 캐시 도입으로 O(N) 선형 탐색 → 평균 O(1) 해시 조회

### (2) Dedicated Server 관련 — "깊이 관여 안 했다"가 아니라 "이 범위는 직접 했다"로 수정
포트폴리오에 본인 트러블슈팅으로 명시된 항목: **HasAuthority() 조건 분기로 서버의 공격 판정과 클라이언트의 시각 효과 실행을 분리해 공격 이펙트 중복 재생 문제 해결**. 이건 GAS 어빌리티 코드 안에서 서버/클라이언트 실행 분기를 처리한 것이지, Replication 프로퍼티 설계나 RPC 구조 자체를 짠 건 아닙니다. 질문 들어오면 이렇게 경계를 명확히 말하세요.
- 할 수 있는 말: "GAS 어빌리티 내부에서 HasAuthority()로 서버/클라이언트 실행을 분기해서 이펙트 중복 재생 문제를 해결한 경험은 있습니다."
- 조심할 말: Replication variable 설계, RPC 함수 직접 설계 경험이 있다는 식의 과장은 피하기. 구체적으로 물어보면 "그 부분은 팀 내 네트워크 담당이 설계했고, 제가 맡은 GAS 로직에서 권한 분기 처리만 직접 했습니다"로 정직하게 범위를 좁혀 답하기.

### (3) JinEngine = Project GGD, 4인 팀, 중간 합류
- 본인 혼자 만든 엔진이 아니라, **이미 개발 중이던 C++ 자체엔진 코드베이스**에 4인 팀 프로젝트로 합류해 게임플레이 기능을 구현하는 포지션
- 자소서 Q4의 "구조와 방향은 제가 먼저 잡고" 표현은 JinEngine 전체가 아니라 **본인이 맡은 기능 단위**(유닛 판매 흐름, 유닛 합성 로직, UI 등)에 한정해서 말해야 앞뒤가 맞음
- 포트폴리오에 본인이 직접 "배운 점"으로 적어둔 내용: 기존 구조를 먼저 파악하고 의도가 불명확한 부분은 기존 개발자에게 질문하며 정해진 흐름 안에서 구현했다는 것 → 이게 오히려 협업 관점에서는 좋은 답이 될 수 있음 (아래 PART 5 참고)

---

## PART 1. 오프닝 — 자기소개 / 지원동기

### Q1. 1분 자기소개 부탁드립니다.
**꼬리질문**: 왜 게임 프로그래머가 되고 싶었나요? / 다른 개발 분야도 있는데 왜 게임인가요?

**모범답안 뼈대**
- 디지펜 게임공학과에서 C++/CS 기초 중심으로 게임 개발 지식을 쌓음
- 학교 프로젝트로 자체 엔진(BIT SAVER), Unity, Unreal, OpenGL 그래픽스(디퍼드 셰이딩) 데모까지 직접 제작
- 졸업 후에도 Unity 모바일 게임("눈덩이를 굴려라") 출시, 개인 프로젝트(콘솔 뱀서라이크)로 공백 없이 개발 지속
- 원티드 언리얼 교육과정에서 LTF 팀 프로젝트 1등, 현재는 Project GGD(JinEngine 기반 팀 프로젝트)에서 게임플레이 기능 구현 중

**압박질문**: "게임을 좋아하는 것과 게임을 잘 만드는 건 다른데, 근거가 뭔가요?"
→ LTF 타일 점유 충돌을 CombatManager 중앙 관리로 해결한 경험, Project GGD에서 유닛 합성 같은 복잡한 상태 관리 로직을 상태 불일치 없이 구현한 경험으로 연결.

---

### Q2. 왜 넥토리얼(넥슨)에 지원했나요?
**꼬리질문**: 다른 회사 아니라 왜 넥슨? / 넥슨 게임 중 좋아하는 게임은?

**모범답안 뼈대**
- 넥슨은 완성 후 끝나는 게임이 아니라 수년간 운영하며 계속 개선하는 회사
- 지금까지는 프로젝트를 "완성"까지 만드는 경험이었다면, 이미 서비스 중인 게임의 코드를 분석하고 최적화하는 넥슨의 방식을 직접 경험하고 싶음
- 실제 서비스 규모의 문제(유저 수, 데이터량, 레거시 코드)를 현업이 어떻게 다루는지 배우고 싶음

**압박질문**: "넥슨 게임 하나 구체적으로 말해보세요, 어떤 시스템이 인상적이었나요?"
→ 본인이 실제 플레이한 넥슨 게임 기준으로 시스템 하나(경제/매칭/콘텐츠 롤아웃 등)를 프로그래머 시점에서 짧게 분석해서 미리 준비.

---

## PART 2. LTF 프로젝트 심층 검증

### Q3. LTF 프로젝트를 설명해주세요.
**꼬리질문**: 본인이 맡은 역할은? / 왜 TFT를 모작 대상으로 골랐나요?

**모범답안 뼈대**
- Unreal 5.6, Dedicated Server, GAS 기반 5인 팀 TFT 모작 오토배틀러, 원티드 교육과정 최종 프로젝트 1위
- 로비, 공동 선택, 상점, 배치, 전투, 크립 라운드, 최종 승패까지 핵심 루프 구현에 참여
- 본인 담당: 유닛 전투 FSM(탐색-타깃선정-사거리판정-이동-공격-스킬-사망), GAS 기반 챔피언 스킬(20종 이상: 투사체/돌진/순간이동/에어본/기절/회복/보호막/버프/다중타깃), HexTile 이동 및 경로 탐색(BFS)

**압박질문**: "스킬 20종이면 꽤 많은데 어떻게 구조화했나요? 하나하나 다 새로 짰나요?"
→ 답변 방향: GA_Attack → GA_AttackHitCheck처럼 연출과 실제 타격 판정을 분리하는 공통 파이프라인을 만들고, GameplayEffect/Tag로 버프·디버프 조합을 데이터 기반으로 구성해서 반복을 줄였다는 논리로 준비.

---

### Q4. 가장 어려웠던 문제는 무엇이었나요?
**꼬리질문**: 기존 방식의 문제점은? / 왜 중앙 관리 방식으로 바꿨나요? / HexTile 조회는 어떻게 개선했나요?

**모범답안 뼈대** (0번 정정 내용 그대로)
1. 문제: 유닛마다 개별적으로 타일 이동 가능 여부 판단 → 유닛 늘수록 예약 정보가 얽혀 빈 타일인데도 이동 정체
2. 해결: CombatManager가 모든 이동 요청을 수집해 요청 순서와 이동 속도 기준으로 승인/대기/재경로를 중앙에서 판정
3. 최적화: 좌표 기반 타일 인덱스 Map + 거리 캐시로 O(N) 선형 탐색 → 평균 O(1) 해시 조회
4. (서버 환경) GAS 어빌리티 내부에서 HasAuthority() 분기로 서버 판정과 클라이언트 이펙트를 분리해 공격 이펙트 중복 재생 문제도 해결

**압박질문**: "요청 순서 기준으로 승인한다고 했는데, 먼저 요청한 유닛이 항상 유리하면 불공평한 상황은 없나요?"
→ 이동 속도도 함께 고려한다고 되어 있으므로, 단순 선착순이 아니라 속도 변수까지 반영해 판정했다는 걸 강조. 구체적으로 속도를 어떻게 가중치로 썼는지는 본인 코드 기준으로 한 번 더 정리해두는 게 좋음.

---

### Q5. 유닛이 훨씬 많아진다면 지금 구조로 버틸 수 있나요?
**모범답안 방향**: 프로파일링으로 병목 확인 → 이동 판단 빈도 줄이기(상태 변화 시에만) → 공간 분할로 근접 유닛만 비교 → 멀티스레딩은 로직 스레드 안전성 비용부터 검토

**압박질문**: "TFT는 유닛이 그렇게 많지 않은데 이 최적화가 과한 것 아닌가요?"
→ 지금 구조는 필요했던 만큼만 최적화한 것이라는 균형 잡힌 답으로 준비.

---

## PART 3. C++ / CS 기초

### Q6. vector와 list 차이는?
- vector: 연속 메모리, 캐시 효율 좋음, random access O(1), 중간 삽입/삭제 O(n)
- list: 노드 기반, 중간 삽입/삭제 O(1), random access 없음
- 게임에서는 순차 접근·캐시 지역성 때문에 대부분 vector 선호

### Q7. new/delete와 malloc/free 차이는?
- new: 생성자 호출, 타입 정보 유지, 실패 시 예외
- malloc: 크기만 할당, 생성자 호출 안 함, 실패 시 NULL

### Q8. virtual 함수와 vtable은 어떻게 동작하나요?
- virtual 함수 있는 클래스는 vtable을 만들고 객체가 vptr로 이를 가리킴, 런타임에 실제 타입의 vtable로 동적 바인딩
- 압박질문: "생성자에서 virtual 함수를 호출하면?" → 아직 파생 클래스 vtable이 세팅되지 않아 base 버전이 호출된다는 점까지 준비

### Q9. Object Pool은 왜 쓰나요? (콘솔 뱀서라이크 프로젝트 연결)
**모범답안**: 콘솔 뱀서라이크 개인 프로젝트에서 총알/적 같은 빈번히 생성/파괴되는 오브젝트에 Object Pool을 적용했는데, prewarm 이후 메모리가 계속 늘어나는 문제가 있었음. GameLevel의 초기화(hasBeginPlay)가 여러 번 호출되던 게 원인이라 초기화가 1회만 보장되도록 분리해서 해결.
**압박질문**: "Quadtree는 왜 도입했고 어떤 문제가 있었나요?"
→ 전환 과정에서 메모리 에러와 충돌 누락이 있었는데, 활성 객체만 필터링하고 Query 흐름을 재정리해서 해결했다는 흐름으로 답변.

---

## PART 4. Unreal Engine & 그래픽스

### Q10. Actor와 Component 차이는?
- Actor: 월드에 배치/스폰되는 기본 단위 / Component: Actor에 붙어 기능을 조합하는 단위

### Q11. GameplayAbility / GameplayEffect / GameplayTag 각각 역할은?
- Ability: 실행 가능한 행동(스킬) 단위 / Effect: 스탯 변화(버프·디버프·데미지)를 데이터 기반으로 적용 / Tag: 상태·조건을 계층 문자열로 표현해 필터링에 사용
- LTF에서 GA_Attack → GA_AttackHitCheck로 연출과 판정을 분리한 사례로 연결

### Q12. 디퍼드 셰이딩(Deferred Shading)은 왜 쓰나요? (Shading Project 연결)
**모범답안**: Geometry Pass에서 Position/Normal/Albedo를 G-Buffer에 담아 MRT로 한 번에 출력하고, Lighting Pass에서 G-Buffer를 샘플링해 광원 연산을 하는 구조. 포워드 셰이딩과 달리 라이트 개수만큼 지오메트리를 다시 그리지 않아도 돼서 라이트가 많을 때 유리함. 반사 벡터(내적/외적 계산)와 큐브맵 샘플링으로 동적 환경 반사도 구현.
**압박질문**: "디퍼드 셰이딩의 단점은?" → 투명 오브젝트 처리가 어렵고, G-Buffer 자체가 메모리/대역폭을 많이 씀.

---

## PART 5. Project GGD(JinEngine) & AI 활용 개발 철학

### Q13. 지금 하고 있는 Project GGD는 어떤 프로젝트인가요?
**모범답안 뼈대**
- C++ 자체엔진(JinEngine) 기반 4인 팀 프로젝트, 이미 개발 중이던 코드베이스에 게임플레이 프로그래머로 합류
- 본인 기여: 유닛 판매(1개/전체) 흐름과 CommandCard 동작, ESC 기반 일시정지/설정 팝업 UI, 라운드 이자 보상, 조기 웨이브 스킵, 아이템 카드 리롤, 유닛 합성(같은 종류 슬롯 등록 → 확률 계산 → 결과 유닛 생성/실패 보상), 툴팁/정보창/바이너리 저장/최고점수 NEW 표시 등

**압박질문**: "이미 만들어진 엔진/프로젝트에 중간 합류했다고 했는데, 처음에 어떻게 코드를 파악했나요?"
→ 모범답안: 기능 파일만 보지 않고 초기화 → Update → Render → Input → 오브젝트 생명주기 → UI 상태 전환 → 저장 데이터 흐름 순서로 구조를 따라가며 파악했고, 의도나 책임 범위가 불명확한 부분은 기존 개발자에게 직접 질문했다는 흐름으로 답변.

### Q14. (앞선 자소서 답변과 연결) "구조와 방향은 본인이 먼저 잡는다"고 했는데, Project GGD는 이미 있던 프로젝트에 합류한 거잖아요?
**모범답안 뼈대** — 범위를 명확히 좁혀서 답하기
- 엔진 전체나 프로젝트 전체 구조는 이미 있던 팀의 것을 따르는 게 맞고, 본인이 말한 원칙은 **본인이 맡은 기능 단위**(유닛 합성 로직, 판매 흐름 등)에 적용한 것
- 예: 유닛 합성 기능을 추가할 때 임시 UI 슬롯 상태와 실제 필드 유닛 상태가 어긋날 수 있는 문제를 먼저 발견하고, 검증-슬롯 등록/해제-결과 생성-필드 복귀 흐름을 분리하는 설계는 본인이 직접 정한 뒤 AI를 보조적으로 활용
→ 이 질문은 앞뒤 안 맞으면 신뢰도가 크게 깎이는 포인트라 반드시 이 프레임으로 정리해서 연습할 것.

### Q15. AI를 개발에 어떻게 활용하나요?
**모범답안 뼈대**
- AI를 코드 생성기가 아니라 같이 검토하는 도구로 사용
- 프로젝트 가이드라인/구조를 문서화해 반복 설명과 토큰 낭비를 줄이고, 진행 상황과 설계 의도를 메모로 남겨 맥락 유지
- AI가 제안한 코드는 왜 이렇게 짰는지, 더 나은 방법은 없는지 반복 질문해서 완전히 설명할 수 있을 때만 적용
- 본인이 맡은 기능 단위의 설계는 먼저 본인이 정하고, 구현 일부만 AI 활용

**압박질문**: "AI가 준 코드를 그대로 썼다가 문제 생긴 적은요?" / "AI 없이도 지금 실력으로 개발 가능한가요?"
→ 구체적인 거부/수정 사례 하나(가능하면 Project GGD의 유닛 합성 로직처럼 상태 관리가 복잡했던 부분)로 답변 준비.

---

## PART 6. 인성 & 협업

### Q16. 본인의 장점과 단점은?
- 장점: 끝까지 파고드는 문제 해결력 → LTF 타일 점유 충돌 구조 변경 사례
- 단점: 깊게 이해하려다 시간이 오래 걸림 → 최근엔 프로토타입으로 방향부터 빠르게 검증하고 이후 깊게 개선하는 식으로 관리 중이라는 개선 포인트까지 붙이기

### Q17. 협업 중 반대 의견이나 솔직한 피드백을 전해야 했던 경험은?
**모범답안 뼈대**
- LTF는 일정이 짧아 프로토타입 먼저, 디테일은 나중이라는 팀 합의
- 한 팀원이 본인 파트를 계속 다듬으며 프로토타입 완성이 늦어짐
- 비난처럼 들리지 않게 따로 1대1로, "처음 정한 목표는 프로토타입 완성"이라는 점을 다시 짚음
- 팀원도 수긍, 이후 프로토타입 먼저 완성 후 다같이 디테일을 채워 일정 회복

**압박질문**: "그때 본인이 나선 이유는? 팀 리더가 따로 있지 않았나요?"
→ 공개 자리 대신 따로 얘기한 이유, "사람"이 아니라 "일정/목표" 프레임으로 접근했다는 점 강조.

### Q18. (Project GGD 관련 새 질문) 중간 합류한 프로젝트에서 기존 개발자와 의견이 다르면 어떻게 하나요?
**모범답안 뼈대**: 기존 흐름과 책임 범위를 먼저 파악하는 게 우선이라고 생각하고, 정말 개선이 필요하다고 판단되면 근거(발견한 상태 불일치 문제 등)를 구체적으로 들고 먼저 질문하는 방식으로 접근한다는 흐름으로, 이미 포트폴리오에 적어둔 "배운 점"을 그대로 살려서 답변.

---

## PART 7. 상황 대응 & 게임관

### Q19. 전투 중 갑자기 프레임 드랍이 발생했습니다. 어떻게 접근하시겠습니까?
원인 확인(재현 조건) → 프로파일링으로 병목 특정(렌더링 vs 로직) → 렌더링이면 DrawCall/LOD/Culling, 로직이면 반복 계산/자료구조 점검 → 수정 → 재측정 검증

### Q20. 재미있는 게임의 조건, 좋은 게임 프로그래머란?
"기획자의 의도를 코드로 구현하되 기능만이 아니라 플레이어가 느끼는 감각까지 고려하는 사람" — 구체적 예시 하나(조작감/반응성이 좋았던 게임) 준비해두기

### Q21. 최근 재미있게 한 게임은?
본인이 실제로 플레이한 게임 기준으로 시스템 하나(경제 밸런스, 매칭, 연출 타이밍 등)를 프로그래머 시점에서 짧게 분석하는 연습. 넥슨 게임이면 좋지만 아니어도 솔직하게.

---

## 최종 점검 체크리스트 (면접 직전)

- [ ] LTF 이동/점유 문제: CombatManager 중앙 관리 / 요청순서+속도 기준 승인·대기·재경로 / 타일 인덱스 Map+거리 캐시, 세 가지를 순서대로 헷갈리지 않고 말할 수 있는가 (bounce-back 표현 쓰지 않기)
- [ ] LTF Dedicated Server: HasAuthority() 분기로 이펙트 중복 재생 해결한 것까지만이 본인 범위라는 걸 명확히 말할 수 있는가
- [ ] Project GGD가 4인 팀 프로젝트이고 본인은 중간 합류했다는 사실을 자연스럽게 인정하면서, "본인이 맡은 기능 단위"에서는 설계를 직접 했다는 걸 구분해서 말할 수 있는가
- [ ] GAS 구성요소(Ability/Effect/Tag) 역할과 GA_Attack→GA_AttackHitCheck 파이프라인 예시
- [ ] "왜 넥슨인가" 답변에 넥슨 게임 구체적 사례 1개 이상 포함
- [ ] AI 활용 원칙 4줄 요약(검토 도구 / 문서화 / 이해 후 적용 / 맡은 범위 안에서 설계는 본인이) 자연스럽게 말할 수 있는가
- [ ] 콘솔 뱀서라이크(Object Pool, Quadtree), Shading Project(디퍼드 셰이딩) 등 다른 프로젝트도 한 줄 요약은 즉시 나오는가
- [ ] 장점/단점 + 갈등 경험을 LTF 사례와 자연스럽게 연결

---

*이전 버전(v1)에서 "bounce-back"과 "JinEngine 솔로 개발" 표현은 포트폴리오 원문과 맞지 않아 정정했습니다.*

---

## PART 8. CS 기초 심화 — 실제 면접 기출 (17문항)

아래 17개는 실제 넥토리얼(또는 유사 회사) 면접에서 그대로 나왔던 질문입니다. 다른 파트보다 우선순위 높게 반복 연습하세요. 본인이 이미 정리해둔 답변은 다듬고, 비어 있던 항목은 채워 넣었습니다.

### Q22. 자체 엔진 개발은 어떤 식으로 했나요?
**모범답안 뼈대**: BIT SAVER(C++/OpenGL, Tech Lead)를 예로 — 싱글톤 기반 코어 매니저(입력, 사운드, 리소스)와 Component 기반 GameObject 구조로 설계. 게임 루프는 Init → Update → Render 구조, GameObject가 Component 포인터를 벡터로 들고 있다가 매 프레임 순회하며 업데이트/드로우를 호출하는 방식.
**꼬리질문 대비**: "왜 싱글톤을 코어 매니저에 썼나요?" → Q23으로 자연스럽게 연결.

### Q23. 싱글톤이란?
**모범답안**: 클래스의 인스턴스를 프로그램 전체에서 하나만 생성되도록 보장하고 전역 접근 지점을 제공하는 디자인 패턴. 게임에서는 InputManager, SoundManager, GameManager처럼 상태가 하나만 존재해야 하는 시스템에 사용.
**압박질문**: "싱글톤의 단점은?" → 전역 상태라 의존성이 숨겨져 테스트하기 어렵고, 멀티스레드 환경에서 lazy initialization을 쓰면 동시에 여러 스레드가 생성자를 호출할 수 있어 thread-safety를 별도로 챙겨야 한다(예: double-checked locking, 혹은 C++11부터는 함수 내 static 변수의 초기화가 thread-safe하다는 점 활용).

### Q24. 자체엔진과 Unity/Unreal 비교
**모범답안 뼈대**
- 자체엔진: 모든 코드를 직접 통제하고 원리를 완전히 이해할 수 있지만, 에디터/에셋 파이프라인/물리/직렬화 등을 전부 직접 만들어야 해서 개발 속도가 느림
- Unity: C# 기반으로 스크립팅이 쉽고 에셋스토어와 반복 속도가 빠르지만, 매니지드 언어의 GC 오버헤드와 엔진 내부에 대한 통제력은 상대적으로 낮음
- Unreal: C++ 기반으로 강력한 내장 시스템(렌더링, 네트워킹, GAS 등)을 제공하고 Blueprint로 빠른 반복이 가능하지만, 엔진 자체가 무겁고 러닝커브가 있음
- 결론: 기초 원리를 배우는 데는 자체엔진이, 실제 서비스 개발 속도에는 상용 엔진이 유리하다는 트레이드오프로 정리

### Q25. 형변환이 반드시 필요한 경우, 실제 사례
**모범답안**: GameObject 리스트를 순회하다가 특정 자식 클래스(예: Monster)만 가진 데이터/함수를 써야 할 때, base 포인터/참조를 dynamic_cast로 다운캐스팅. 포인터면 nullptr 체크, 참조면 bad_cast 예외로 실패를 처리. static_cast는 타입이 확실할 때만 쓰고, RTTI 비용이 부담되면 자체 타입 태그(enum)로 대체하는 방법도 있다는 것까지 언급하면 좋음.

### Q26. 다형성이란?
**모범답안**: 같은 인터페이스로 다른 동작을 하게 만드는 성질. 컴파일 타임 다형성(오버로딩 — 같은 이름, 다른 매개변수/반환형)과 런타임 다형성(오버라이딩 — virtual 함수로 파생 클래스가 재정의)으로 나뉨. 실전에서는 base 클래스 포인터로 다양한 파생 클래스 객체를 다루면서 virtual 함수 하나로 각자 다른 동작을 하게 만드는 패턴을 주로 사용.

### Q27. 가상함수가 동작하는 원리
**모범답안**: virtual 함수를 가진 클래스는 컴파일러가 vtable(가상 함수 테이블)을 생성하고, 객체는 vptr로 이 테이블을 가리킴. 함수 호출 시 vptr을 통해 실제 객체 타입의 vtable에서 오버라이드된 함수를 찾아 호출 — 런타임 바인딩.
**압박질문**: "생성자에서 virtual 함수를 호출하면?" → 아직 파생 클래스 vtable이 세팅 전이라 base 버전이 호출됨.

### Q28. 스마트 포인터 — 개념, 트레이드오프, 그리고 실제 사례
**기술 개념 답변 (일반 지식으로 준비)**
- 주로 shared_ptr을 사용해봤고, reference count로 객체 생명주기를 관리하는 방식이라는 것까지는 이해하고 있음
- 트레이드오프: reference count 증감이 atomic 연산이라 호출이 매우 잦아지면 성능에 영향을 줄 수 있음. 예를 들어 `for (auto obj : list<shared_ptr<T>>)`처럼 값으로 순회하면 매 반복마다 복사 생성자가 호출되어 ref count 증가/감소가 반복되는데, `for (const auto& obj : ...)`처럼 참조로 순회하면 복사 생성자 호출 자체를 없애 오버헤드를 줄일 수 있음
- unique_ptr과의 구분 기준: 소유권이 하나뿐이면 unique_ptr, 여러 시스템이 동시에 참조해야 하면 shared_ptr

> ⚠️ 이 내용은 넥슨 개발 블로그에서 본 트러블슈팅 사례이지 본인이 직접 겪은 문제가 아닙니다. "실제로 겪은 경험"처럼 말하지 말고, "이런 원리로 문제가 생기고 이렇게 해결한다"는 지식 답변으로만 준비하세요. "직접 겪어본 적 있나요?"라고 구체적으로 물으면 본인이 실제 프로젝트(LTF, Project GGD 등)에서 스마트 포인터를 쓴 진짜 사례로 답해야 합니다 — 어떤 프로젝트에서 shared_ptr/unique_ptr을 어디에 썼는지 지금 한 번 코드 기준으로 정리해두는 걸 권장합니다.

**Q2(왜 넥슨인가)에 추가할 내용**
- 넥슨 개발 블로그에서 스마트 포인터의 atomic reference count 과다 호출로 인한 렉 이슈를 실제 서비스 환경에서 어떻게 발견하고 해결했는지 다룬 글을 인상 깊게 읽었음
- "이런 디테일한 성능 이슈까지 실제 서비스에서 마주치고, 그 과정을 공유하는 개발 문화"에 대한 관심으로 연결하면 지원 동기가 훨씬 구체적으로 들림
- 압박질문 대비: "그 글 제목이나 정확한 내용 기억나세요?" 나올 수 있으니, 면접 전에 해당 글을 다시 한번 찾아 읽고 핵심 포인트(증상 발견 경위, 원인, 해결 방법)를 본인 말로 요약해두는 게 안전함

### Q29. 삼각형 내부의 점 판별
**모범답안**: 삼각형의 세 변을 같은 방향(예: 시계 방향)으로 놓고, 각 변의 벡터와 (변의 시작점 → 판별할 점) 벡터의 외적을 구해 부호를 확인. 세 외적의 부호가 모두 같으면 점이 삼각형 내부에 있음. (barycentric 좌표로도 같은 문제를 풀 수 있다는 것까지 언급하면 좋음)

### Q30. 물리 메모리보다 큰 데이터를 다뤄야 한다면?
**모범답안**: 가상 메모리를 활용. 프로세스는 논리(가상) 주소 공간을 쓰고, 실제 물리 메모리에는 필요한 페이지만 적재됨. 현재 메모리에 없는 페이지에 접근하면 page fault가 발생하고 OS가 디스크(스왑)에서 해당 페이지를 불러옴. 자주 쓰는 페이지는 RAM에 유지되도록 page replacement 알고리즘(LRU 등)이 관리. 키워드: RAM/디스크, 페이지, 캐시, page fault, page replacement.

### Q31. 32비트와 64비트의 차이
**모범답안**: 주소 공간 크기 차이(2^32 ≈ 4GB vs 2^64), 레지스터/포인터 폭 차이(4바이트 vs 8바이트)로 한 번에 처리 가능한 데이터 크기와 최대 주소 지정 가능 메모리량이 달라짐. 포인터가 커지는 만큼 메모리 사용량도 늘 수 있다는 트레이드오프도 언급.

### Q32. 정렬 알고리즘 종류와 복잡도, vector/list에 어울리는 정렬
**모범답안**
- 대표 정렬: 버블(O(n²)), 삽입(O(n²), 거의 정렬된 데이터에 강함), 퀵(평균 O(n log n), 최악 O(n²)), 병합(O(n log n), 안정 정렬, 추가 메모리 필요), 힙(O(n log n))
- vector: random access가 빠르고 캐시 지역성이 좋아 퀵정렬 계열이 유리 — std::sort는 실제로 introsort(퀵+힙+삽입 하이브리드)
- list: random access가 없어 퀵정렬의 파티셔닝이 비효율적 — 순차 접근만으로 되는 병합 정렬이 적합, 실제로 std::list::sort는 병합 정렬로 구현됨

### Q33. Component Based Development로 뱀서라이크를 만든다면?
**모범답안**: 좋다고 생각함 — 이유는 조합의 유연한 확장성과 밸런싱 편의성. 자체엔진에서 Component 추상 클래스를 만들고 GameObject가 벡터로 Component 포인터들을 들고 Update/Draw를 위임하는 구조를 실제로 써봤고, 새 오브젝트나 난이도 조절이 필요할 때 컴포넌트 단위로 쉽게 추가/교체할 수 있었음. Unreal에서도 컴포넌트별로 기능을 나누고 Blueprint에서 수치를 빠르게 조절했던 경험과 같은 맥락 — 뱀서라이크처럼 수많은 아이템/스킬 조합이 필요한 장르에는 CBD가 특히 유리하다는 논리로 마무리.

### Q34. 그래픽스 파이프라인 설명
**모범답안**: CPU에서 정점 데이터를 준비(VAO/VBO) → Vertex Shader(정점별 변환) → Primitive Assembly(정점을 삼각형 등으로 조립) → Rasterization(래스터화, 픽셀 후보 생성) → Fragment/Pixel Shader(픽셀별 색상 계산) → Output Merging(깊이/스텐실 테스트, 블렌딩) 순서. 본인의 디퍼드 셰이딩 프로젝트(G-Buffer, MRT)와 자연스럽게 연결해서 설명하면 좋음.

### Q35. Real-Time Rendering 책에서 가장 인상 깊었던 부분
→ 보류. 그래픽스보다는 다른 강점(CS 기초, 프로젝트 경험) 위주로 답변 방향을 잡기로 했으므로 지금은 비워둡니다. 이 질문이 실제로 나오면 짧게 아는 선에서 답하고 다음 질문으로 넘어가는 정도로만 대비하면 됩니다.

### Q36. 각 자료구조에 대한 깊은 이해
**모범답안 — 표로 정리해서 암기**

| 자료구조 | 탐색 | 삽입/삭제 | 특징 |
|---|---|---|---|
| Array/Vector | O(1) | 끝 O(1), 중간 O(n) | 연속 메모리, 캐시 효율 최고 |
| Linked List | O(n) | O(1) (위치를 알 때) | 노드 기반, 삽입/삭제 빠름 |
| Stack/Queue | - | O(1) | LIFO/FIFO, 함수 호출·BFS 등에 사용 |
| Hash Table | 평균 O(1), 최악 O(n) | 평균 O(1) | 해시 충돌 시 성능 저하, 체이닝/오픈어드레싱으로 처리 |
| BST(이진탐색트리) | 평균 O(log n), 최악 O(n) | 평균 O(log n) | 불균형 시 성능 저하 → 균형트리(AVL, Red-Black)로 보완 |
| Heap | - | O(log n) | 최댓값/최솟값 빠른 접근, 우선순위 큐에 사용 |
| Graph | 구현에 따라 다름 | - | 인접 리스트(O(V+E) 공간, 희소 그래프에 유리) vs 인접 행렬(O(V²), 밀집 그래프/빠른 엣지 조회에 유리) |

**압박질문**: "게임에서 실제로 해시 테이블을 어디에 썼나요?" → LTF HexTile 좌표 기반 타일 인덱스 Map(해시 조회)로 연결해서 답변.

---

### PART 8 추가 체크리스트
- [ ] Q28 스마트 포인터 답변에서 "넥슨 블로그에서 본 사례"와 "본인이 실제 프로젝트에서 쓴 경험"을 절대 섞어 말하지 않는가 — 후자는 실제 코드 기준으로 별도 준비했는가
- [ ] Q32 정렬 복잡도표를 안 보고 바로 말할 수 있는가
- [ ] Q35는 보류 상태 — 나오면 짧게만 답하고 넘어가기로 함
- [ ] Q36 표를 외워서 즉석에서 자료구조별 Big-O를 말할 수 있는가
