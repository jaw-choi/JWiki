share_ptr은 heap공간이 살아 있는지 검증하는 능력 없기 때문에 공간이 살아있는지 아닌지 판단 할 수 있는 weak_ptr와 함께 사용

std::unique_ptr<Player> player2 = std::move(player1);
이후에 player1 접근하면 안됨(Dangling Pointer)

make_shared하면 생성자 호출해줌

shared_ptr(강한참조) 참조횟수 0 되면 해제
			->순환참조문제-> weak_ptr사용

weak_ptr -> lock 제공(유효하면 객체얻어와지고 아니면 nullptr얻음)
			