### 요약
함수 포인터로 시작한 콜백 개념이 함수 객체(펑터)와 std::function, 람다로 확장되면서 더 안전하고 유연한 형태로 발전했고, STL 알고리즘에서 그 진가가 드러남

### 콜백과 시스템 프로그래밍

- 1.운영체제 API나 그래픽스, 윈도우 시스템 프로그래밍에서는 “내가 만든 함수를 시스템에 등록해두고, 시스템이 나중에 거꾸로 호출하는” 패턴이 매우 많음
    
- C#에서는 이를 delegate로 부르지만, C++에서는 함수 포인터/함수 객체/람다 등으로 같은 목적(로직 전달)을 달성함
    

2. 함수 포인터 핵심   

- 함수의 주소를 변수에 담아 “로직 자체를 인자로 넘기는” 수단
    
- 문법이 어렵게 느껴지는 이유는 타입 표기가 특이하고, 선언 위치가 일반 변수 선언과 다르기 때문
    
- 그래서 실무에서는 typedef 또는 using으로 “함수 포인터 타입 별칭”을 만들어 깔끔하게 쓰는 방식이 흔함
    

3. std::function의 위치    

- 함수 포인터처럼 “호출 가능한 대상”을 담는 래퍼(함수 래퍼/타입 소거 개념)
    
- 다만 만능으로 아무거나 다 되는 느낌이라기보다는, 결국 호출 가능한 대상을 담아 호출하는 구조이며 내부적으로 비용/특성이 있음
    
- 실무에서 편의성 때문에 자주 쓰지만, 성능/할당/인라이닝 관점은 따로 고려 포인트가 됨
    

4. [[람다]]
    

- “함수 선언 + 구현”을 호출 지점에 바로 적어서 로직 전달 단계를 줄임
    
- STL 알고리즘(정렬, 검색, 삭제 조건 등)에 특히 잘 어울림
    
- []캡처를 통해 상태를 들고 다닐 수 있어, 단발성 조건/짧은 로직에 매우 편리함
    
- 다만 상태가 복잡해지면 람다 한 줄이 과해져서 가독성이 떨어질 수 있음
    

5. 함수 객체(펑터)의 정체와 장점
    

- 클래스에 operator() (호출 연산자)를 오버로딩해서 “객체인데 함수처럼 호출되는” 형태
    
- 핵심 장점: 상태를 멤버 변수로 자연스럽게 유지 가능  
    예: 호출 횟수 카운팅, 누적값 유지, 설정값 기반 비교 등
    
- 람다 도입 이전에는 STL에서 펑터가 매우 널리 쓰였고, 지금도 “상태/구조/재사용”이 필요하면 펑터가 더 직관적인 경우가 많음
    

6. STL 알고리즘에서의 활용 포인트
    

- remove_if 같은 알고리즘: 조건(프레디케이트)을 넘겨서 특정 원소 제거
    
- sort 같은 알고리즘: 비교 함수(컴페어)를 넘겨서 정렬 기준 정의
    
- 이때 비교/조건을 함수 포인터로도 줄 수 있고, 펑터로도 줄 수 있고, 요즘은 람다를 가장 많이 사용
    

7. std::sort 내부 정렬이 “하이브리드(인트로소트)”인 이유(강의 후반 내용)
    

- 표준 라이브러리는 한 가지 정렬만 고집하지 않고, 데이터 크기/상황에 따라 여러 알고리즘을 섞어 성능과 최악 케이스를 관리함
    
>- *[[CS - 정렬 알고리즘]]
>- 대표적으로 인트로소트(introsort): 평균적으로 빠른 ==퀵정렬==(피벗) 계열의 장점 + 최악 케이스 방지 - ==힙정렬==(트리 구조,heapify) + 작은 구간에서는 ==삽입정렬==(돌면서 자기 자리 찾기) 활용 같은 식의 혼합 전략*
    
- 결론: 우리는 sort만 호출하지만, 그 뒤에서는 “상황별 최적 선택”이 이미 구현되어 있음
    

마지막 정리

- 함수 포인터: 로직 전달의 원형, 콜백의 기본
    
- 함수 객체(펑터): 로직 + 상태를 객체로 깔끔하게 묶는 방식
    
- 람다: 로직 전달을 가장 간단히 만드는 현대적 표기(단발성 조건에 강함)
    
- std::function: 호출 가능한 대상을 편하게 담는 범용 래퍼
    
- STL 알고리즘은 “로직을 인자로 받는 구조”라서 위 개념들이 실제로 가장 자주 쓰이는 무대임